<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>机器学习工程 on Xuan</title><link>https://blog.430812.xyz/mlops/ml-engineering/</link><description>Recent content in 机器学习工程 on Xuan</description><generator>Hugo</generator><language>zh-CN</language><lastBuildDate>Sun, 09 Aug 2026 00:00:00 +0800</lastBuildDate><atom:link href="https://blog.430812.xyz/mlops/ml-engineering/index.xml" rel="self" type="application/rss+xml"/><item><title>引言</title><link>https://blog.430812.xyz/mlops/ml-engineering/01/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/01/</guid><description>&lt;h1 id="引言"&gt;引言&lt;/h1&gt;
&lt;p&gt;虽然本书的读者应当已具备机器学习（machine learning）的基础知识，但仍有必要从定义入手，以确保我们对全书使用的术语有一致的理解。&lt;/p&gt;
&lt;p&gt;下面，我复述《百页机器学习书》（The Hundred-Page Machine Learning Book）第 2 章中的部分定义，并给出若干新定义。如果你读过我的第一本书，本章的某些内容听起来可能会很熟悉。&lt;/p&gt;
&lt;p&gt;读完本章后，我们将对监督学习（supervised learning）与无监督学习（unsupervised learning）等概念达成一致的理解；我们将统一数据术语，例如直接使用与间接使用的数据、原始数据（raw data）与整洁数据（tidy data）、训练数据与留出数据。&lt;/p&gt;
&lt;p&gt;我们将知道何时该用机器学习、何时不该用，以及机器学习的各种形式，如基于模型的学习（model-based learning）与基于实例的学习（instance-based learning）、深度学习（deep learning）与浅层学习（shallow learning）、分类（classification）与回归（regression）等。&lt;/p&gt;
&lt;p&gt;最后，我们将界定机器学习工程（Machine Learning Engineering，MLE）的范畴，并介绍机器学习项目生命周期。&lt;/p&gt;
&lt;h2 id="11-记号与定义"&gt;1.1 记号与定义&lt;/h2&gt;
&lt;p&gt;让我们先说明基本的数学记号，并定义本书中会反复使用的术语与概念。&lt;/p&gt;
&lt;h3 id="111-数据结构"&gt;1.1.1 数据结构&lt;/h3&gt;
&lt;p&gt;标量（scalar）是简单的数值，如 15 或 -3.25。&lt;sup id="fnref:1"&gt;&lt;a href="#fn:1" class="footnote-ref" role="doc-noteref"&gt;1&lt;/a&gt;&lt;/sup&gt; 取标量值的变量或常量用斜体字母表示，如 \(x\) 或 \(a\)。&lt;/p&gt;
&lt;p&gt;向量（vector）是标量值的有序列表，这些标量值称为属性（attribute）。我们用粗体字符表示向量，例如 \(\mathbf{x}\) 或 \(\mathbf{w}\)。向量既可以可视化为指向某些方向的箭头，也可以可视化为多维空间中的点。图 1 给出了三个二维向量 \(\mathbf{a} = [2, 3]\)、\(\mathbf{b} = [-2, 5]\) 和 \(\mathbf{c} = [1, 0]\) 的插图。我们用带索引的斜体值表示向量的属性，如 \(w^{(j)}\) 或 \(x^{(j)}\)。索引 \(j\) 表示向量的特定维度，即属性在列表中的位置。例如，在图 1 中用红色显示的向量 \(\mathbf{a}\) 中，\(a^{(1)} = 2\)，\(a^{(2)} = 3\)。&lt;/p&gt;
&lt;p&gt;图 1：将三个向量分别可视化为方向与点。&lt;/p&gt;</description></item><item><title>项目开始之前</title><link>https://blog.430812.xyz/mlops/ml-engineering/02/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/02/</guid><description>&lt;h1 id="项目开始之前"&gt;项目开始之前&lt;/h1&gt;
&lt;p&gt;在一个机器学习项目开始之前，必须先对它进行优先级排序（prioritization）。优先级排序是不可避免的：团队和设备的能力有限，而组织积压的项目可能很长。&lt;/p&gt;
&lt;p&gt;要对项目进行优先级排序，就必须估算其复杂度。对于机器学习，准确的复杂度估算几乎不可能做到，因为存在重大的未知因素，例如：所需的模型质量在实践中是否能够达到、需要多少数据、需要哪些特征以及多少特征。&lt;/p&gt;
&lt;p&gt;此外，机器学习项目必须有明确定义的目标。基于项目的目标，可以恰当地调整团队并配置资源。&lt;/p&gt;
&lt;p&gt;在本章中，我们讨论这些以及相关的、必须在机器学习项目开始之前处理好的活动。&lt;/p&gt;
&lt;h2 id="21-机器学习项目的优先级排序"&gt;2.1 机器学习项目的优先级排序&lt;/h2&gt;
&lt;p&gt;机器学习项目优先级排序的关键考量是影响（impact）和成本（cost）。&lt;/p&gt;
&lt;h3 id="211-机器学习的影响"&gt;2.1.1 机器学习的影响&lt;/h3&gt;
&lt;p&gt;在更广泛的工程项目中使用机器学习，其影响是高的，当：1) 机器学习可以取代工程项目中的一个复杂部分，或 2) 获得廉价（但可能不完美）的预测能带来巨大的好处。&lt;/p&gt;
&lt;p&gt;例如，现有系统的一个复杂部分可能是基于规则的（rule-based），包含许多嵌套规则和例外情况。构建和维护这样的系统可能极其困难、耗时且容易出错。当软件工程师被要求维护系统的该部分时，它也可能成为重大挫败感的来源。这些规则能否通过学习获得，而不是逐条编写？能否利用现有系统轻松生成标注数据（labeled data）？如果可以，这样的机器学习项目将具有高影响和低成本。&lt;/p&gt;
&lt;p&gt;廉价且不完美的预测可能很有价值，例如在调度大量请求的系统中。假设许多此类请求是&amp;quot;简单&amp;quot;的，可以用某些现有自动化快速解决。其余请求被认为是&amp;quot;困难&amp;quot;的，必须人工处理。&lt;/p&gt;
&lt;p&gt;一个基于机器学习的系统，如果能够识别&amp;quot;简单&amp;quot;任务并调度给自动化处理，将为人类节省大量时间，他们只需把精力和时间集中在困难请求上。即使调度器在预测中出错，困难请求也会到达自动化环节，自动化会在它上面失败，人类最终仍会收到该请求。如果人类错误地收到了一个简单请求，也没有问题：这个简单请求仍然可以发送给自动化，或由人类处理。&lt;/p&gt;
&lt;h3 id="212-机器学习的成本"&gt;2.1.2 机器学习的成本&lt;/h3&gt;
&lt;p&gt;三个因素极大地影响机器学习项目的成本：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;问题的难度，&lt;/li&gt;
&lt;li&gt;数据的成本，以及&lt;/li&gt;
&lt;li&gt;对准确率（accuracy）的需求。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;以正确的数量获得正确的数据可能非常昂贵，特别是涉及人工标注（manual labeling）时。对高准确率的需求可能转化为获取更多数据或训练更复杂模型的要求，例如深度神经网络（deep neural network）的独特架构或非平凡的集成架构（ensembling architecture）。&lt;/p&gt;
&lt;p&gt;当你考虑问题的难度时，主要的考量是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;是否有已实现的算法或软件库能够解决该问题（如果有，问题就大大简化了），&lt;/li&gt;
&lt;li&gt;是否需要大量的计算能力来构建模型或在生产环境中运行它。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;成本的第二个驱动因素是数据。必须做出以下考量：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;数据能否自动生成（如果可以，问题就大大简化了），&lt;/li&gt;
&lt;li&gt;人工标注数据的成本是多少（即为未标注示例分配标签（label）的成本），&lt;/li&gt;
&lt;li&gt;需要多少示例（通常这无法提前知道，但可以从已知的已发表结果或组织自身的经验中估算）。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;最后，最具影响力的成本因素之一是模型的期望准确率。机器学习项目的成本随准确率要求超线性增长，如图 1 所示。当模型部署到生产环境中时，低准确率也可能成为重大损失的来源。需要考量的有：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;每一次错误预测的代价有多大，以及&lt;/li&gt;
&lt;li&gt;模型变得不切实际的最低准确率水平是多少。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="22-估算机器学习项目的复杂度"&gt;2.2 估算机器学习项目的复杂度&lt;/h2&gt;
&lt;p&gt;对于机器学习项目，除了与组织执行过的其他项目或文献中报道的项目进行比较之外，没有标准的复杂度估算方法。&lt;/p&gt;
&lt;h3 id="221-未知因素"&gt;2.2.1 未知因素&lt;/h3&gt;
&lt;p&gt;有几个主要的未知因素，除非你过去做过类似项目或读过相关项目，否则几乎不可能有把握地猜测。这些未知因素是：&lt;/p&gt;
&lt;p&gt;图 1：成本随准确率要求的超线性增长。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/02_000.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;所需的质量在实践中是否能够达到，&lt;/li&gt;
&lt;li&gt;要达到所需质量需要多少数据，&lt;/li&gt;
&lt;li&gt;需要哪些特征以及多少特征，才能使模型充分学习和泛化，&lt;/li&gt;
&lt;li&gt;模型应该有多大（这对神经网络和集成架构尤其重要），以及&lt;/li&gt;
&lt;li&gt;训练一个模型需要多长时间（换句话说，运行一次实验需要多少时间），以及达到期望性能水平需要多少次实验。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;有一件事你几乎可以肯定：如果所需的模型准确率（我们将在第 5 章的 ?? 节中讨论的一种流行的模型质量指标）高于 99%，你可以预期会出现与标注数据数量不足相关的复杂情况。在某些问题中，即使 95% 的准确率也被认为很难达到。（当然，这里我们假设数据是平衡的，即不存在类别不平衡（class imbalance）。我们将在下一章的 ?? 节中讨论类别不平衡。）&lt;/p&gt;
&lt;p&gt;另一个有用的参照是人类在该任务上的表现。如果你希望模型表现得和人类一样好，这通常是一个难题。&lt;/p&gt;
&lt;h3 id="222-简化问题"&gt;2.2.2 简化问题&lt;/h3&gt;
&lt;p&gt;做出更有根据的猜测的一种方法是简化问题，先解决一个更简单的问题。例如，假设问题是把一组文档分类到 1000 个主题中。先做一个试点项目，只关注 10 个主题，把属于其他 990 个主题的文档视为&amp;quot;其他&amp;quot;（Other）。1 为这 11 个类别（10 个真实主题，加上&amp;quot;其他&amp;quot;）人工标注数据。这里的逻辑是，人类只需记住 10 个主题的定义，比记住 1000 个主题之间的区别要简单得多。2&lt;/p&gt;</description></item><item><title>数据收集与准备</title><link>https://blog.430812.xyz/mlops/ml-engineering/03/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/03/</guid><description>&lt;h1 id="数据收集与准备"&gt;数据收集与准备&lt;/h1&gt;
&lt;p&gt;在任何机器学习（machine learning）活动开始之前，分析师必须收集并准备数据。分析师可获得的数据并不总是「正确」的，也不总是机器学习算法可以直接使用的形式。本章聚焦于机器学习项目生命周期的第二个阶段，如下图所示：&lt;/p&gt;
&lt;p&gt;图 1：机器学习项目生命周期。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/03_000.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;具体来说，我们将讨论优质数据的属性、数据集可能存在的典型问题，以及为机器学习准备和存储数据的方法。&lt;/p&gt;
&lt;h2 id="31-关于数据的问题"&gt;3.1 关于数据的问题&lt;/h2&gt;
&lt;p&gt;既然你已经有了一个机器学习目标，并且明确定义了模型的输入、输出和成功标准，你就可以开始收集训练模型（model）所需的数据了。然而，在开始收集数据之前，有一些问题需要回答。&lt;/p&gt;
&lt;h3 id="311-数据是否可获取"&gt;3.1.1 数据是否可获取？&lt;/h3&gt;
&lt;p&gt;你所需的数据是否已经存在？如果存在，它是否可获取（从物理、合同、伦理或成本角度）？如果你要购买或复用他人的数据源，你是否考虑过这些数据可能被如何使用或共享？你是否需要与原始供应商重新谈判许可协议？&lt;/p&gt;
&lt;p&gt;如果数据可获取，它是否受版权或其他法律规范保护？如果是，你是否已确定你的数据版权归谁所有？是否存在共同版权？&lt;/p&gt;
&lt;p&gt;数据是否敏感（例如，涉及你所在组织的项目、客户或合作伙伴，或者被政府列为机密）？是否存在潜在的隐私问题？如果是，你是否已与数据采集的受访者讨论过数据共享？你是否能长期保存个人信息，以便将来使用？&lt;/p&gt;
&lt;p&gt;你是否需要将数据与模型一起共享？如果是，你是否需要获得数据所有者或受访者的书面同意？&lt;/p&gt;
&lt;p&gt;你是否需要在分析过程中或为共享做准备时对数据进行匿名化处理&lt;sup id="fnref:1"&gt;&lt;a href="#fn:1" class="footnote-ref" role="doc-noteref"&gt;1&lt;/a&gt;&lt;/sup&gt;，例如去除个人身份信息（personally identifiable information，PII）？&lt;/p&gt;
&lt;p&gt;即使从物理上获取所需数据是可能的，在上述所有问题解决之前，也不要使用这些数据。&lt;/p&gt;
&lt;h3 id="312-数据是否足够大"&gt;3.1.2 数据是否足够大？&lt;/h3&gt;
&lt;p&gt;你希望得到明确答案的问题是：数据是否足够。然而，正如我们已经发现的，通常并不知道需要多少数据才能达成目标，尤其是当最低模型质量要求很严格时。&lt;/p&gt;
&lt;p&gt;如果你对短期内是否有足够数据存疑，那么请弄清楚新数据的生成频率。对于某些项目，你可以先使用最初可用的数据开始，当你进行特征工程（feature engineering）、建模并解决其他相关技术问题的同时，新数据可能会逐渐到来。这些数据可以自然产生（作为某个可观测或可测量过程的结果），也可以由你的数据标注专家或第三方数据提供商逐步提供。&lt;/p&gt;
&lt;p&gt;考虑完成项目所需的预计时间。在这段时间内能否收集到足够大的数据集&lt;sup id="fnref:2"&gt;&lt;a href="#fn:2" class="footnote-ref" role="doc-noteref"&gt;2&lt;/a&gt;&lt;/sup&gt;？请基于类似项目的经验或文献中报告的结果来回答这个问题。&lt;/p&gt;
&lt;p&gt;判断你是否收集了足够数据的一个实用方法是绘制学习曲线（learning curves）。更具体地说，绘制你的学习算法在不同训练样本数量下的训练得分和验证得分，如图 2 所示。&lt;/p&gt;
&lt;p&gt;通过观察学习曲线，你会发现当训练样本达到一定数量后，模型性能将趋于平稳。达到该训练样本数量后，继续增加样本带来的收益将开始递减。&lt;/p&gt;
&lt;p&gt;图 2：朴素贝叶斯（Naïve Bayes）学习算法应用于 scikit-learn 标准「digits（手写数字）」数据集的学习曲线。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/03_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;如果你观察到学习算法的性能趋于平稳，这可能表明收集更多数据对训练更好的模型没有帮助。我使用了「可能」这个表述，因为还有另外两种可能的解释：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;你没有足够的信息性特征（informative features），供学习算法利用以构建性能更强的模型；或者&lt;/li&gt;
&lt;li&gt;你使用的学习算法无法利用现有数据训练出足够复杂的模型。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在前一种情况下，你可以考虑通过以巧妙的方式组合现有特征，或利用间接数据源（如查找表（lookup tables）和地名录（gazetteers））的信息来工程化更多特征。我们将在第 4 章的 ?? 节讨论合成特征的技术。&lt;/p&gt;
&lt;p&gt;在后一种情况下，一种可行的方法是使用集成学习（ensemble learning）方法或训练深度神经网络。然而，深度神经网络通常比浅层学习算法需要更多的训练数据。&lt;/p&gt;
&lt;p&gt;一些从业者使用经验法则来估计一个问题所需的训练样本数。通常，他们会使用应用于以下对象的缩放因子：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;特征数量，或&lt;/li&gt;
&lt;li&gt;类别数量，或&lt;/li&gt;
&lt;li&gt;模型中可训练参数的数量。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这类经验法则通常有效，但它们因问题领域而异。每位分析师都会根据自己的经验调整数字。虽然你需要通过经验来发现适合自己的那些「神奇」缩放因子，但各种网络来源中最常被引用的数字是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;特征数量的 10 倍（这往往会夸大训练集的大小，但作为上界效果很好），&lt;/li&gt;
&lt;li&gt;类别数量的 100 或 1000 倍（这往往会低估大小），或&lt;/li&gt;
&lt;li&gt;可训练参数数量的 10 倍（通常应用于神经网络）。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;请记住，仅仅因为你有大数据，并不意味着你应该全部使用。大数据中较小的样本在实践中也能给出良好的结果，并加速寻找更好模型的过程。不过，重要的是要确保样本能代表整个大数据集。分层抽样（stratified sampling）和系统抽样（systematic sampling）等抽样策略可以带来更好的结果。我们将在 3.10 节讨论数据抽样策略。&lt;/p&gt;</description></item><item><title>特征工程</title><link>https://blog.430812.xyz/mlops/ml-engineering/04/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/04/</guid><description>&lt;h1 id="特征工程"&gt;特征工程&lt;/h1&gt;
&lt;p&gt;在数据收集与准备之后，特征工程（feature engineering）是机器学习中第二重要的活动。它也是机器学习项目生命周期的第三个阶段：&lt;/p&gt;
&lt;p&gt;图 1：机器学习项目生命周期。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/04_000.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;特征工程是一个过程：首先在概念上、然后在程序上，将一条原始示例（raw example）转换为特征向量（feature vector）。它包含构思一个特征，然后编写程序代码，将整条原始示例——可能借助一些间接数据——转换为该特征。&lt;/p&gt;
&lt;h2 id="41-为什么要做特征工程"&gt;4.1 为什么要做特征工程&lt;/h2&gt;
&lt;p&gt;更具体地说，考虑在推文中识别电影标题的问题。假设你有一大堆电影标题；这是可以间接使用的数据。你还有一堆推文；这些数据将直接用于创建示例。首先，为电影标题构建一个索引，以便快速进行字符串匹配。&lt;sup id="fnref:1"&gt;&lt;a href="#fn:1" class="footnote-ref" role="doc-noteref"&gt;1&lt;/a&gt;&lt;/sup&gt; 然后在你的推文中找出所有电影标题的匹配项。现在规定你的示例就是这些匹配项，而你的机器学习问题是二分类（binary classification）：一个匹配项是电影，还是不是电影。&lt;/p&gt;
&lt;p&gt;考虑下面这条推文：&lt;/p&gt;
&lt;p&gt;图 2：Kyle 的一条推文。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/04_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;我们的电影标题匹配索引会帮助我们找到以下匹配项：「avatar」「the terminator」「It」和「her」。这就给了我们四个未标记的示例。你可以给这四个示例打上标签：{(avatar, False), (the terminator, True), (It, False), (her, False)}。然而，机器学习算法无法仅从电影标题本身学到任何东西（人类也不能）：它需要上下文。你可能会决定，匹配项之前的五个词和之后的五个词构成一个信息量足够的上下文。用机器学习的行话来说，我们称这样的上下文为匹配项周围的「十字窗口」（ten-word window）。你可以把窗口的宽度作为超参数（hyperparameter）来调整。&lt;/p&gt;
&lt;p&gt;现在，你的示例是带有上下文的已标记匹配项。然而，学习算法无法直接应用于这样的数据。机器学习算法只能应用于特征向量。这就是你求助于特征工程的原因。&lt;/p&gt;
&lt;h2 id="42-如何做特征工程"&gt;4.2 如何做特征工程&lt;/h2&gt;
&lt;p&gt;特征工程是一个创造性的过程，分析师在此过程中运用他们的想象力、直觉和领域专长。在我们这个在推文中识别电影标题的示例问题中，我们凭直觉把匹配项周围的窗口宽度固定为十。现在，我们需要更有创造力，才能把字符串序列转换成数值向量。&lt;/p&gt;
&lt;h3 id="421-文本的特征工程"&gt;4.2.1 文本的特征工程&lt;/h3&gt;
&lt;p&gt;说到文本，科学家和工程师经常使用简单的特征工程技巧。其中两个技巧是独热编码（one-hot encoding）和词袋（bag-of-words）。&lt;/p&gt;
&lt;p&gt;一般来说，独热编码将一个类别属性（categorical attribute）转换成若干个二值属性。假设你的数据集有一个属性「颜色」（Color），可能的取值为「红」「黄」「绿」。我们把每个取值转换成一个三维二值向量，如下所示：&lt;/p&gt;
\[
\text{red} \to (1, 0, 0), \quad \text{yellow} \to (0, 1, 0), \quad \text{green} \to (0, 0, 1)
\]&lt;p&gt;在电子表格中，你将不再使用一个以「颜色」为表头的列，而是使用三个合成列，取值为 1 或 0。这样做的好处是，你现在可以使用大量的机器学习算法，因为只有少数学习算法支持类别属性。&lt;/p&gt;
&lt;p&gt;词袋是独热编码技术应用于文本数据的一种推广。你不是用这种技术来表示一个属性，而是用它将整个文本文档表示为一个二值向量。让我们看看它是如何工作的。&lt;/p&gt;
&lt;p&gt;想象你有一个包含六篇文本文档的集合，如下所示：&lt;/p&gt;
&lt;p&gt;图 3：一个包含六篇文档的集合。&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;文档 1&lt;/th&gt;
					&lt;th&gt;Love, love is a verb&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;文档 2&lt;/td&gt;
					&lt;td&gt;Love is a doing word&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;文档 3&lt;/td&gt;
					&lt;td&gt;Feathers on my breath&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;文档 4&lt;/td&gt;
					&lt;td&gt;Gentle impulsion&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;文档 5&lt;/td&gt;
					&lt;td&gt;Shakes me, makes me lighter&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;文档 6&lt;/td&gt;
					&lt;td&gt;Feathers on my breath&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;假设你的问题是构建一个按主题分类的文本分类器。分类学习算法期望输入是带标签的特征向量，因此你必须将文本文档集合转换为特征向量集合。词袋就能让你做到这一点。&lt;/p&gt;</description></item><item><title>监督式模型训练</title><link>https://blog.430812.xyz/mlops/ml-engineering/05/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/05/</guid><description>&lt;h1 id="监督式模型训练第一部分"&gt;监督式模型训练（第一部分）&lt;/h1&gt;
&lt;p&gt;模型训练（model training，又称建模，modeling）是机器学习项目生命周期的第四个阶段：&lt;/p&gt;
&lt;p&gt;图 1：机器学习项目生命周期。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/05_000.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;显然，没有训练，就不会构建出任何模型。然而，模型训练是机器学习中最被高估的活动之一。平均而言，机器学习工程师只把 5%–10% 的时间花在建模上，甚至可能更少。成功的数据收集、准备和特征工程（feature engineering）更为重要。通常，建模只是把 scikit-learn 或 R 中的某个算法应用到你的数据上，再随机尝试几组超参数（hyperparameter）组合而已。所以，如果你跳过了前面两章直接跳到建模，请回去读一读那些章节，它们很重要。&lt;/p&gt;
&lt;p&gt;正如本章标题所示，我把监督式模型训练分成了两部分。在第一部分中，我们将讨论学习准备、选择学习算法、浅层学习策略、评估模型性能、偏差-方差权衡（bias-variance tradeoff）、正则化（regularization）、机器学习流水线（pipeline）的概念，以及超参数调优（hyperparameter tuning）。&lt;/p&gt;
&lt;h2 id="51-开始建模之前"&gt;5.1 开始建模之前&lt;/h2&gt;
&lt;p&gt;在着手模型之前，你应该验证数据与模式的一致性，定义可达到的性能水平，选择性能指标，并做出其他几项决策。&lt;/p&gt;
&lt;h3 id="511-验证数据符合模式"&gt;5.1.1 验证数据符合模式&lt;/h3&gt;
&lt;p&gt;首先，确保数据符合模式文件（schema file）所定义的模式。即使数据最初是由你准备的，原始数据和当前数据也很可能不一样。这种差异可以由多种因素解释，最可能的是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;用于将数据持久化到硬盘或数据库的方法包含错误；&lt;/li&gt;
&lt;li&gt;其他人可能在未通知你的情况下更改了数据或模式；&lt;/li&gt;
&lt;li&gt;你用来从持久化位置读取数据的方法包含错误。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些模式错误必须像检测编程代码错误一样被检测、识别和纠正。如有必要，应从头重新运行整个数据收集和准备流水线，正如我们在第 3 章末尾讨论可复现性（reproducibility）时所说的那样。&lt;/p&gt;
&lt;h3 id="512-定义可达到的性能水平"&gt;5.1.2 定义可达到的性能水平&lt;/h3&gt;
&lt;p&gt;定义可达到的性能水平是至关重要的一步。它让你知道什么时候该停止尝试改进模型。以下是一些指导原则：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果一个人不需要太多努力、数学或复杂的逻辑推导就能标注示例，那么你可以期望你的模型达到人类水平的性能；&lt;/li&gt;
&lt;li&gt;如果输入特征向量包含大量信号（如图像中的像素或文档中的单词），你可以期望接近零误差；&lt;/li&gt;
&lt;li&gt;如果做出标注决策所需的信息完全包含在特征中，你可以期望接近零误差；&lt;/li&gt;
&lt;li&gt;如果你有一个能解决相同分类或回归问题的计算机程序，你可以期望你的模型至少表现得一样好。通常，随着更多带标签数据的到来，机器学习模型的性能还可以提高；以及&lt;/li&gt;
&lt;li&gt;如果你观察到一个相似但不同的系统，你可以期望得到一个相似但不同的机器学习模型性能。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="513-选择性能指标"&gt;5.1.3 选择性能指标&lt;/h3&gt;
&lt;p&gt;我们稍后会讨论如何评估模型性能。目前，有几种指标（metrics）可以用来估计模型性能的水平（即其质量）。没有一个适用于所有项目的最佳指标。你将根据你的数据和问题来选择。&lt;/p&gt;
&lt;p&gt;建议你在开始建模之前选择一个且仅一个性能指标。然后，使用这一个指标来比较不同的模型并跟踪整体进度。&lt;/p&gt;
&lt;p&gt;在第 5.5 节中，你将读到最流行、最实用的模型性能指标，以及将多个指标组合成单一数值的方法。&lt;/p&gt;
&lt;p&gt;图 2：模型性能与人类性能基线的比较：(a) 模型看起来不错，因此我们可以决定对其进行正则化或添加更多训练示例；(b) 模型表现不佳，因此我们需要添加更多特征或增加模型复杂度。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/05_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;h3 id="514-选择合适的基线"&gt;5.1.4 选择合适的基线&lt;/h3&gt;
&lt;p&gt;在开始构建预测模型之前，为你的问题建立一个基线（baseline）性能很重要。基线是提供比较参考点的模型或算法。&lt;/p&gt;
&lt;p&gt;拥有基线能让分析师确信基于机器学习的解决方案是有效的。如果机器学习模型的性能指标值优于使用基线获得的值，那么机器学习就提供了价值。&lt;/p&gt;
&lt;p&gt;将当前模型的性能与基线进行比较，可以引导工作走向不同的方向。假设我们知道我们的问题可以达到人类水平的性能。那么我们就把人类性能作为基线，如图 2 所示。在图 2a 中，模型看起来不错，因此我们可以决定对其进行正则化或添加更多训练示例。另一方面，在图 2b 中，模型表现不佳，因此我们应该添加更多特征或增加模型复杂度（model complexity）。&lt;/p&gt;
&lt;p&gt;基线是接收输入并输出预测的模型或算法。基线的预测输出必须与模型的预测具有相同的性质。否则，你无法比较它们。&lt;/p&gt;
&lt;p&gt;基线不一定是任何学习算法的结果。它可以是一个基于规则或启发式的算法、应用于训练数据的简单统计量，或者其他什么东西。&lt;/p&gt;
&lt;p&gt;两种最常用的基线算法是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;随机预测（random prediction），以及&lt;/li&gt;
&lt;li&gt;零规则（zero rule）。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;随机预测算法通过从分配给训练示例的标签集合中随机选择一个标签来做出预测。在分类问题中，它对应于从问题的所有类别中随机挑选一个类别。在回归问题中，它意味着从训练数据的所有唯一目标值中选择一个。&lt;/p&gt;</description></item><item><title>监督式模型训练</title><link>https://blog.430812.xyz/mlops/ml-engineering/06/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/06/</guid><description>&lt;h1 id="监督式模型训练第-2-部分"&gt;监督式模型训练（第 2 部分）&lt;/h1&gt;
&lt;p&gt;在我们关于监督式模型训练（supervised model training）讨论的第二部分中，我们将考虑以下主题：训练深度模型（deep model）、模型堆叠（model stacking）、处理类别不平衡数据集、分布偏移（distribution shift）、模型校准（model calibration）、故障排查（troubleshooting）与误差分析（error analysis），以及其他最佳实践。&lt;/p&gt;
&lt;p&gt;与浅层模型（shallow model）相比，深度神经网络（deep neural network）的模型训练策略涉及更多可变的环节。另一方面，它也更有章可循，更适合自动化。&lt;/p&gt;
&lt;h2 id="61-深度模型训练策略"&gt;6.1 深度模型训练策略&lt;/h2&gt;
&lt;p&gt;模型训练始于筛选出几种网络架构，也称为网络拓扑（network topology）。如果你处理的是图像数据，并且想从头构建模型，那么一个默认的拓扑选择可能是：一个卷积神经网络（convolutional neural network，CNN），它至少包含一个卷积层（convolutional layer）、一个紧随其后的最大池化层（max-pooling layer）和一个全连接层（fully connected layer）。&lt;/p&gt;
&lt;p&gt;如果你处理的是文本或其他序列数据（如时间序列），你可以在 CNN、门控循环神经网络（gated recurrent neural network）（例如长短期记忆（Long Short Term Memory，LSTM）或门控循环单元（gated recurrent unit，GRU））或 Transformer 之间进行选择。&lt;/p&gt;
&lt;p&gt;除了从头训练模型，你还可以从预训练模型（pre-trained model）开始。像 Google 和 Microsoft 这样的公司已经训练了非常深的神经网络，其架构针对图像或自然语言处理任务进行了优化。&lt;/p&gt;
&lt;p&gt;在图像处理任务中使用最多的预训练模型包括：VGG16 和 VGG19（基于视觉几何组（Visual Geometry Group，VGG）架构）、InceptionV3（基于 GoogLeNet 架构）和 ResNet50（基于残差网络（residual network）架构）。&lt;/p&gt;
&lt;p&gt;对于自然语言文本处理，与从头训练模型相比，诸如基于 Transformer 架构的 BERT（Bidirectional Encoder Representations from Transformer，来自 Transformer 的双向编码器表示）和基于双向 LSTM 架构的 ELMo（Embeddings from Language Models，来自语言模型的嵌入）等预训练模型往往能提高模型质量。&lt;/p&gt;
&lt;p&gt;使用预训练模型的一个好处是，这些模型是在其创建者可以获得、但你很可能无法获得的海量数据上训练的。即使你的数据集更小，且与预训练模型所用的数据集不完全相似，预训练模型学到的参数仍然可能有用。&lt;/p&gt;
&lt;p&gt;你可以通过两种方式使用预训练模型：&lt;/p&gt;</description></item><item><title>模型评估</title><link>https://blog.430812.xyz/mlops/ml-engineering/07/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/07/</guid><description>&lt;h1 id="模型评估"&gt;模型评估&lt;/h1&gt;
&lt;p&gt;统计模型在现代组织中扮演着越来越重要的角色。当应用于业务场景时，模型可能会影响组织的财务指标。然而，它也可能带来责任风险。因此，任何在生产环境中运行的模型都必须被仔细且持续地评估。&lt;/p&gt;
&lt;p&gt;模型评估（model evaluation）是机器学习项目生命周期的第五个阶段：&lt;/p&gt;
&lt;p&gt;图 1：机器学习项目生命周期。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/07_000.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;根据模型的应用领域以及组织的业务目标和约束，模型评估可能包括以下任务：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;评估将模型投入生产可能带来的法律风险。例如，某些模型预测可能间接传达机密信息。网络攻击者或竞争对手可能试图对模型的训练数据进行逆向工程。此外，当用于预测时，某些特征（如年龄、性别或种族）可能导致组织被认为存在偏见甚至歧视。&lt;/li&gt;
&lt;li&gt;研究训练数据与生产数据分布的主要性质。通过比较训练数据和生产数据中示例、特征和标签的统计分布，可以检测出分布偏移（distribution shift）。两者之间的显著差异表明需要更新训练数据并重新训练模型。&lt;/li&gt;
&lt;li&gt;评估模型的性能。在模型部署到生产环境之前，必须在其外部数据（即未用于训练的数据）上评估其预测性能。外部数据必须同时包含来自生产环境的历史示例和在线示例。在实时在线数据上进行评估的上下文应尽可能接近生产环境。&lt;/li&gt;
&lt;li&gt;监控已部署模型的性能。模型的性能可能随时间下降。能够检测到这一点，并通过添加新数据升级模型或训练一个完全不同的模型，这一点非常重要。模型监控必须是一个精心设计的自动化过程，并且可能包含人在回路（human in the loop）。我们将在第9章中更详细地讨论这一点。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在本章中，我们来看一些统计学家在模型评估阶段使用的技巧示例。机器学习工程是一门发展中的学科，有些问题仍然没有既成熟又易于应用的答案。特别是，本评估是从工程师的角度呈现的，而每个业务都有自己的成功标准，这些标准是独一无二的。在评估机器学习解决方案之前，非常重要的一点是确保合适的人已经完成了项目中最困难的工作：弄清楚成功是什么样的，以及以适合业务的形式（指标和目标）该问哪些正确的问题。&lt;/p&gt;
&lt;p&gt;一个常见的失败原因是工程师用基本工具回答方便的问题，而不是用定制工具回答正确的问题——这可能需要在你项目的领导者和利益相关者完成他们在项目中的部分之后咨询专业统计学家。请注意，本章中强调的一些方法，特别是用于 A/B 测试（7.2 节）的方法，仅作为示例提供，可能不适用于你的特定业务问题。在重要的大型项目上，试图自己做所有事情是一个错误。及时与你的领导团队协作并咨询统计学家至关重要。&lt;/p&gt;
&lt;h2 id="71-离线与在线评估"&gt;7.1 离线与在线评估&lt;/h2&gt;
&lt;p&gt;在第 ? 节中，我们概述了应用于所谓离线模型评估（offline model evaluation）的评估技术。离线模型评估发生在分析人员训练模型时。分析人员尝试不同的特征、模型、算法和超参数。诸如混淆矩阵（confusion matrix）和各种性能指标（如精确率（precision）、召回率（recall）和 AUC）之类的工具，允许比较候选模型，并引导模型训练朝着正确的方向进行。&lt;/p&gt;
&lt;p&gt;首先，使用验证数据（validation data）来评估所选的性能指标并比较模型。一旦确定了最佳模型，就使用测试集（test set），也以离线模式，再次评估最佳模型的性能。这最终的离线评估保证了部署后的模型性能。在本章中，除其他主题外，我们将讨论为模型的离线测试性能建立统计界限（statistical bounds）。&lt;/p&gt;
&lt;p&gt;本章的很大一部分专门讨论在线模型评估（online model evaluation），即使用在线数据在生产环境中测试和比较模型。离线与在线模型评估之间的区别，以及每种评估类型在机器学习系统中的位置，如图 2 所示。&lt;/p&gt;
&lt;p&gt;在图 2 中，历史数据首先用于训练一个部署候选（deployment candidate）。然后对其进行离线评估，如果结果令人满意，部署候选就成为已部署模型（deployed model），并开始接受用户查询。然后，用户查询和模型预测用于模型的在线评估。在线数据随后用于改进模型。为了闭合循环，在线数据会被永久复制到离线数据仓库中。&lt;/p&gt;
&lt;p&gt;图 2：离线与在线模型评估在机器学习系统中的位置。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/07_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;为什么我们要同时进行离线评估和在线评估？离线模型评估反映了分析人员在找到正确的特征、学习算法、模型和超参数值方面取得了多大成功。换句话说，离线模型评估反映了模型从工程角度来看有多好。&lt;/p&gt;
&lt;p&gt;另一方面，在线评估侧重于衡量业务成果，例如客户满意度、平均在线时长、打开率和点击率。这些信息可能不会反映在历史数据中，但这是业务真正关心的。此外，离线评估不允许我们在某些只能在线上观察到的条件下测试模型，例如连接和数据丢失以及调用延迟。&lt;/p&gt;
&lt;p&gt;只有在数据分布随时间保持不变的情况下，在历史数据上获得的性能结果在部署后才会保持有效。然而，在实践中，情况并非总是如此。分布偏移的典型示例包括移动或在线应用程序用户不断变化的兴趣、金融市场的不稳定性、气候变化，或者模型旨在预测其属性的机械系统的磨损。&lt;/p&gt;
&lt;p&gt;因此，模型一旦部署到生产环境就必须持续监控。当发生分布偏移时，必须用新数据更新模型并重新部署。进行此类监控的一种方法是比较模型在在线数据和历史数据上的性能。如果在线数据上的性能与历史数据相比显著变差，就该重新训练模型了。&lt;/p&gt;
&lt;p&gt;在线评估有不同形式，每种形式服务于不同的目的。例如，运行时监控（runtime monitoring）是检查正在运行的系统是否满足运行时要求。&lt;/p&gt;
&lt;p&gt;另一个常见场景是监控用户对不同版本模型的响应行为。此场景中使用的一种流行技术是 A/B 测试（A/B testing）。我们将系统用户分成两组：A 和 B。两组分别被提供服务的是旧模型和新模型。然后我们应用统计显著性检验来决定新模型的性能是否优于旧模型。&lt;/p&gt;
&lt;p&gt;多臂老虎机（multi-armed bandit，MAB）是另一种流行的在线模型评估技术。与 A/B 测试类似，它通过将模型候选暴露给一小部分用户来识别性能最佳的模型。然后，它逐渐将最佳模型暴露给更多用户，同时持续收集性能统计数据，直到其可靠为止。&lt;/p&gt;
&lt;h2 id="72-ab-测试"&gt;7.2 A/B 测试&lt;/h2&gt;
&lt;p&gt;A/B 测试是最常用的统计技术之一。当应用于在线模型评估时，它使我们能够回答诸如「新模型 \(m_B\) 在生产环境中是否比现有模型 \(m_A\) 表现得更好？」或「两个模型候选中哪一个在生产环境中表现得更好？」之类的问题。&lt;/p&gt;</description></item><item><title>模型部署</title><link>https://blog.430812.xyz/mlops/ml-engineering/08/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/08/</guid><description>&lt;h1 id="模型部署"&gt;模型部署&lt;/h1&gt;
&lt;p&gt;一旦模型构建完成并经过充分测试，就可以进行部署了。部署（deploy）模型意味着让模型能够接受生产系统用户发来的查询。生产系统收到查询后，会将查询转换为特征向量（feature vector）。特征向量随后被发送给模型作为评分（scoring）的输入。评分结果再返回给用户。&lt;/p&gt;
&lt;p&gt;模型部署是机器学习项目生命周期中的第六个阶段：&lt;/p&gt;
&lt;p&gt;图 1：机器学习项目生命周期。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/08_000.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;训练好的模型可以通过多种方式进行部署。它可以部署在服务器上，也可以部署在用户的设备上。它可以一次性面向所有用户部署，也可以只面向一小部分用户部署。下面我们逐一考虑这些选项。&lt;/p&gt;
&lt;p&gt;模型可以按照以下几种模式（pattern）进行部署：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;静态部署（static deployment），作为可安装软件包的一部分；&lt;/li&gt;
&lt;li&gt;在用户设备上动态部署（dynamic deployment）；&lt;/li&gt;
&lt;li&gt;在服务器上动态部署；或&lt;/li&gt;
&lt;li&gt;通过模型流式部署（model streaming）。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="81-静态部署"&gt;8.1 静态部署&lt;/h2&gt;
&lt;p&gt;机器学习模型的静态部署与传统软件部署非常相似：你需要准备整个软件的可安装二进制文件。模型被打包为运行时可用的一种资源。根据操作系统和运行时环境的不同，模型对象和特征提取器（feature extractor）对象都可以打包成动态链接库（Windows 上的 DLL）、共享对象（Linux 上的 *.so 文件）的一部分，或者被序列化后保存在基于虚拟机（如 Java 和 .Net）系统的标准资源位置中。&lt;/p&gt;
&lt;p&gt;静态部署有很多优点：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;软件可以直接访问模型，因此对用户来说执行速度快；&lt;/li&gt;
&lt;li&gt;预测时无需将用户数据上传到服务器；这既节省时间，又保护隐私；&lt;/li&gt;
&lt;li&gt;用户离线时也可以调用模型；以及&lt;/li&gt;
&lt;li&gt;软件供应商无需操心让模型保持可用；这变成了用户的责任。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;然而，静态部署也有几个缺点。首先也是最重要的一点，机器学习代码与应用代码之间的关注点分离（separation of concerns）并不总是清晰的。这使得在不升级整个应用的情况下升级模型变得更加困难。其次，如果模型评分有特定的计算需求（例如需要访问加速器或 GPU），这可能会增加复杂性和困惑，让人难以判断静态部署在哪些场景可用、哪些场景不可用。&lt;/p&gt;
&lt;h2 id="82-用户设备上的动态部署"&gt;8.2 用户设备上的动态部署&lt;/h2&gt;
&lt;p&gt;设备上的动态部署与静态部署类似，都是用户在自己的设备上以软件应用的形式运行系统的一部分。区别在于，在动态部署中，模型不是应用二进制代码的一部分。因此它可以实现更好的关注点分离。推送模型更新时无需更新运行在用户设备上的整个应用。此外，动态部署还可以让同一段代码根据可用的计算资源选择合适的模型。&lt;/p&gt;
&lt;p&gt;动态部署可以通过以下几种方式实现：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;部署模型参数；&lt;/li&gt;
&lt;li&gt;部署序列化对象（serialized object）；以及&lt;/li&gt;
&lt;li&gt;部署到浏览器。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="821-模型参数的部署"&gt;8.2.1 模型参数的部署&lt;/h3&gt;
&lt;p&gt;在这种部署场景中，模型文件只包含学习到的参数，而用户的设备上安装了模型的运行时环境。一些机器学习软件包，如 TensorFlow，有可以在移动设备上运行的轻量级版本。&lt;/p&gt;
&lt;p&gt;另外，像 Apple 的 Core ML 这样的框架允许在 Apple 设备上运行用流行软件包（包括 scikit-learn、Keras 和 XGBoost）创建的模型。&lt;/p&gt;
&lt;h3 id="822-序列化对象的部署"&gt;8.2.2 序列化对象的部署&lt;/h3&gt;
&lt;p&gt;在这种方式下，模型文件是一个序列化对象，应用会对其反序列化。这种方法的优点是你不需要在用户设备上为模型准备运行时环境。所有需要的依赖都会随着模型对象一起被反序列化。&lt;/p&gt;
&lt;p&gt;一个明显的缺点是，更新可能会相当“沉重”，如果你的软件系统有数百万用户，这会是个问题。&lt;/p&gt;
&lt;h3 id="823-部署到浏览器"&gt;8.2.3 部署到浏览器&lt;/h3&gt;
&lt;p&gt;大多数现代设备都可以访问浏览器，无论是桌面端还是移动端。一些机器学习框架，如 TensorFlow.js，提供了支持在浏览器中使用 JavaScript 作为运行时来训练和运行模型的版本。&lt;/p&gt;</description></item><item><title>模型服务、监控与维护</title><link>https://blog.430812.xyz/mlops/ml-engineering/09/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/09/</guid><description>&lt;h1 id="模型服务监控与维护"&gt;模型服务、监控与维护&lt;/h1&gt;
&lt;p&gt;在本章中，我们讨论在生产环境中服务（serving）、监控和维护模型的最佳实践。这是机器学习项目生命周期的最后三个阶段：&lt;/p&gt;
&lt;p&gt;图 1：机器学习项目生命周期。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/09_000.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;具体来说，我们将刻画机器学习运行时（runtime，即输入数据被应用于模型的环境）的属性，以及模型服务的模式（modes），例如批处理（batch）和按需（on demand）。此外，我们还将考虑在现实世界中服务模型所面临的三大挑战：错误、变化和人性。我们将描述在生产环境中应该监控什么，以及何时以及如何更新模型。&lt;/p&gt;
&lt;h2 id="91-模型服务运行时的属性"&gt;9.1 模型服务运行时的属性&lt;/h2&gt;
&lt;p&gt;模型服务运行时是将模型应用于输入数据的环境。运行时属性由模型部署模式（deployment pattern）决定。然而，一个有效的运行时还应具备我们在这里讨论的几个额外属性。&lt;/p&gt;
&lt;h2 id="911-安全性与正确性"&gt;9.1.1 安全性与正确性&lt;/h2&gt;
&lt;p&gt;运行时负责认证（authenticate）用户的身份，并授权（authorize）他们的请求。需要检查的事项包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;特定用户是否拥有运行其想运行的模型的授权访问权限，&lt;/li&gt;
&lt;li&gt;传入的参数名称和值是否与模型的规范（specification）相符，以及&lt;/li&gt;
&lt;li&gt;这些参数及其值当前是否对用户可用。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="912-易于部署"&gt;9.1.2 易于部署&lt;/h2&gt;
&lt;p&gt;运行时必须允许以最小的代价更新模型，并且理想情况下不影响整个应用程序。如果模型以 Web 服务的形式部署在物理服务器上，那么模型更新必须简单到只需将一个模型文件替换为另一个模型文件，然后重启 Web 服务即可。&lt;/p&gt;
&lt;p&gt;如果模型是以虚拟机实例或容器的形式部署的，那么运行旧版本模型的实例或容器应该可以逐步替换：逐渐停止正在运行的实例，并从新的镜像启动新实例。同样的原则也适用于编排（orchestrated）容器。&lt;/p&gt;
&lt;p&gt;通常，基于流式处理（streaming-based）的模型应用程序是通过流式传输新版本的模型来更新的。为此，流式应用程序必须是有状态的（stateful）。一旦新版本及相关组件（如特征提取器和评分代码）被流式传输到应用程序中，应用程序的状态就会改变，现在包含这些资产的新版本。现代流处理引擎支持有状态应用程序。所描述的架构如图 2 所示。&lt;/p&gt;
&lt;p&gt;图 2：模型流式传输的高层架构。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/ml-engineering/09_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;h2 id="913-模型有效性的保证"&gt;9.1.3 模型有效性的保证&lt;/h2&gt;
&lt;p&gt;一个有效的运行时会自动确保它所执行的模型是有效的。此外，它还要确保模型、特征提取器和其他组件保持同步。它必须在 Web 服务或流式应用程序每次启动时进行验证，并在运行期间定期验证。如第 8 章第??节所述，每个模型部署时应附带以下四项资产：端到端测试集（end-to-end set）、置信测试集（confidence test set）、性能指标（performance metric）及其可接受取值范围（range of acceptable values）。&lt;/p&gt;
&lt;p&gt;在以下两种情况下，模型不应在生产环境中提供服务（如果正在运行，必须立即停止）：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;至少有一个端到端测试示例没有被正确评分，或&lt;/li&gt;
&lt;li&gt;在置信测试集示例上计算的指标值不在可接受范围内。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="914-易于恢复"&gt;9.1.4 易于恢复&lt;/h2&gt;
&lt;p&gt;有效的运行时允许通过回滚到以前的版本来轻松地从错误中恢复。&lt;/p&gt;
&lt;p&gt;从失败的部署中恢复应该以与部署更新模型相同的方式、相同的容易程度进行。唯一的区别是，不是部署新模型，而是部署之前正常工作的版本。&lt;/p&gt;
&lt;h2 id="915-避免训练服务偏差"&gt;9.1.5 避免训练/服务偏差&lt;/h2&gt;
&lt;p&gt;强烈建议避免使用两套不同的代码库，一套用于训练模型，另一套用于生产环境中的评分。就特征提取（feature extraction）而言，特征提取器代码两个版本之间哪怕微小的差异，都可能导致模型性能欠佳甚至不正确。&lt;/p&gt;
&lt;p&gt;工程团队可能出于多种原因在生产环境中重新实现特征提取器代码。最常见的原因是数据分析师的代码效率低下或与生产生态系统不兼容。&lt;/p&gt;
&lt;p&gt;因此，运行时应该允许方便地访问特征提取代码，以满足各种需求，包括模型再训练（retraining）、临时模型调用（ad-hoc model calls）和生产环境。实现方式之一是将特征提取对象包装成一个独立的 Web 服务。&lt;/p&gt;
&lt;p&gt;如果无法避免使用两套不同的代码库来生成训练和生产所用的特征，那么运行时应该允许记录在生产环境中生成的特征值。这些值随后应被用作训练值。&lt;/p&gt;
&lt;h2 id="916-避免隐藏的反馈回路"&gt;9.1.6 避免隐藏的反馈回路&lt;/h2&gt;
&lt;p&gt;在第 4 章第??节中，我们看到了一个隐藏反馈回路（hidden feedback loop）的例子。模型 \(m_B\) 使用模型 \(m_A\) 的输出作为特征，却不知道模型 \(m_A\) 也使用了模型 \(m_B\) 的输出作为其特征。&lt;/p&gt;</description></item><item><title>结论</title><link>https://blog.430812.xyz/mlops/ml-engineering/10/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/ml-engineering/10/</guid><description>&lt;h1 id="结论"&gt;结论&lt;/h1&gt;
&lt;p&gt;到 2020 年，机器学习（machine learning）已经成为解决商业问题的成熟且流行的工具。过去只有少数组织才能拥有、被其他组织视为&amp;quot;魔法&amp;quot;的能力，如今一个典型的组织也能创建和使用它。&lt;/p&gt;
&lt;p&gt;得益于开源代码、众包、唾手可得的书籍、在线课程以及公开可用的数据集，许多科学家、工程师甚至家庭爱好者现在都可以训练机器学习模型。如果你运气好，你的问题或许只需写几行代码就能解决，正如许多在线教程所演示的那样。&lt;/p&gt;
&lt;p&gt;然而，在一个机器学习项目中，很多环节都可能出错。其中大多数与技术的成熟度无关，也与分析师对机器学习算法的理解程度无关。&lt;/p&gt;
&lt;p&gt;机器学习教材、在线教程和课程都致力于解释机器学习算法的工作原理以及如何将它们应用于数据集。而你的成功很可能取决于其他因素。你能获得哪些数据、能否获得足够的数据、如何为学习准备数据、你设计了哪些特征、你的解决方案是否可扩展、可维护、不会被攻击者操纵、不会犯下代价高昂的错误——这些因素对于一个应用型机器学习项目来说重要得多。&lt;/p&gt;
&lt;p&gt;然而，尽管这些方面至关重要，大多数现代机器学习书籍和课程却常常把它们留给读者自学。有些只提供了部分覆盖，且仅仅针对某个具体示例问题的应用。&lt;/p&gt;
&lt;p&gt;这是知识上的一个重大空白，而本书正是我试图填补这一空白的尝试。&lt;/p&gt;
&lt;h2 id="101-要点"&gt;10.1 要点&lt;/h2&gt;
&lt;p&gt;读完这本书，我希望读者带走什么？&lt;/p&gt;
&lt;p&gt;首先，是深刻理解所有机器学习项目都是独一无二的。不存在一条放之四海而皆准的配方。大多数时候，最大的挑战必须在输入 &lt;code&gt;from sklearn.linear_model import LogisticRegression&lt;/code&gt; 之前解决：你必须定义目标、选择基线（baseline）、收集相关数据、用高质量的标签为其标注，并将带标签的数据转换为训练集、验证集和测试集。问题的其余部分则在输入 &lt;code&gt;model.fit(X,y)&lt;/code&gt; 之后解决，通过应用错误分析、评估模型、验证它能解决问题并且优于现有解决方案。&lt;/p&gt;
&lt;p&gt;经验丰富的分析师或机器学习工程师明白，并非所有问题——无论是商业问题还是其他问题——都能用机器学习解决。事实上，许多问题用启发式方法（heuristic）、数据库查找或传统软件开发就能更轻松地解决。如果系统的每一个动作、决策或行为都必须能够被解释，那么你很可能不应该使用机器学习。除极少数例外，机器学习模型都是黑盒（blackbox）。它们不会告诉你为什么做出了这样的预测，为什么今天没有预测出昨天预测的结果，也不知道如何修复这些问题。&lt;/p&gt;
&lt;p&gt;此外，除非你能找到一个公开数据集和一个开源解决方案，恰好提供你所需的一切，否则机器学习并非实现最短上市时间（time to market）的正确方法。有时，训练和维护模型所需的数据太难获取，甚至根本无法获取。&lt;/p&gt;
&lt;p&gt;另一方面，训练数据也可以通过过采样（oversampling）和数据增强（data augmentation）来合成生成。这些技术常在数据呈现不平衡时使用。&lt;/p&gt;
&lt;p&gt;在开始收集数据之前，先问自己这些问题：你的数据是否可获取、规模足够、可用、可理解、可靠？好的数据包含足够的信息用于建模，对生产用例有良好的覆盖且偏见很少，规模足够大以允许泛化，并且不是模型本身的产物。&lt;/p&gt;
&lt;p&gt;还是说，你的数据伴随着高成本、偏见、不平衡、属性缺失和/或标签噪声？数据质量必须在用于训练之前得到保证。&lt;/p&gt;
&lt;p&gt;机器学习项目生命周期由以下阶段组成：目标定义、数据收集与准备、特征工程、模型训练、评估、部署、服务、监控和维护。在大多数阶段，都可能出现数据泄漏（data leakage）。分析师必须能够预见并防止它。&lt;/p&gt;
&lt;p&gt;在数据准备之后，特征工程是第二重要的阶段。对于某些数据，例如自然语言文本，可以使用词袋（bag-of-words）等技术批量生成特征。然而，最有用的特征往往是分析师凭借领域知识手工打造的。把自己放到&amp;quot;模型的立场&amp;quot;上思考。&lt;/p&gt;
&lt;p&gt;好的特征具有较高的预测能力，可以快速计算，可靠且不相关。它们是单一的，易于理解和维护。特征提取代码是机器学习系统最重要的组成部分之一。它必须经过广泛而系统的测试。&lt;/p&gt;
&lt;p&gt;最佳实践包括：对特征进行缩放，将其存储并记录在模式文件（schema file）或特征存储（feature store）中，并保持代码、模型和训练数据的同步。&lt;/p&gt;
&lt;p&gt;你可以通过离散化现有特征、对训练样本进行聚类，以及对现有特征应用简单变换或将它们两两组合，来合成新特征。&lt;/p&gt;
&lt;p&gt;在开始构建模型之前，确保数据符合模式，然后将其分成三个集合：训练集、验证集和测试集。定义一个可达到的性能水平，并选择一个性能指标。它应该把模型性能归结为单个数字。&lt;/p&gt;
&lt;p&gt;大多数机器学习算法、模型和流水线都有超参数（hyperparameter）。它们会显著影响学习结果。然而，这些参数并非从数据中学习而来。分析师在超参数调优（hyperparameter tuning）过程中设置它们的值。特别是，调整这些值可以控制两个重要的权衡：精确率-召回率（precision-recall）权衡和偏差-方差（bias-variance）权衡。通过改变模型的复杂度，你可以达到所谓的&amp;quot;解区&amp;quot;（zone of solutions），即偏差和方差都相对较低的状态。优化性能指标的解决方案通常可以在解区附近找到。网格搜索（grid search）是最简单、使用最广泛的超参数调优技术。&lt;/p&gt;
&lt;p&gt;与其从头训练一个深度模型，不如从一个预训练模型开始，这往往很有用。使用预训练模型来构建你自己的模型称为迁移学习（transfer learning）。深度模型支持迁移学习这一事实，是它最重要的特性之一。&lt;/p&gt;
&lt;p&gt;训练深度模型可能很棘手。从数据准备到定义神经网络拓扑结构，实现错误可能出现在许多环节。建议从小处着手。例如，使用高层库实现一个简单模型。将默认超参数值应用于一个适合内存的小型归一化数据集。一旦你有了第一个简单的模型架构和数据集，暂时进一步缩小训练数据集，缩小到一个迷你批次（minibatch）的大小。然后开始训练。确保你的简单模型能够过拟合这个训练迷你批次。&lt;/p&gt;
&lt;p&gt;你的机器学习系统的性能可能受益于模型堆叠（model stacking）。理想情况下，用于堆叠的基础模型来自性质不同的算法或模型，例如随机森林（random forest）、梯度提升（gradient boosting）、支持向量机（support vector machine）和深度模型。许多现实世界的生产系统都基于堆叠模型。&lt;/p&gt;
&lt;p&gt;机器学习模型的错误既可以是均匀的，即以相同的比率适用于所有用例；也可以是集中的，即更频繁地出现在某些用例上。通过修复一个集中错误，你只需修一次，就能解决许多示例的问题。&lt;/p&gt;
&lt;p&gt;模型性能可以通过以下简单、迭代的过程来改进：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;使用目前确定的最优超参数值构建模型。&lt;/li&gt;
&lt;li&gt;将模型应用于验证集的一小部分进行测试。&lt;/li&gt;
&lt;li&gt;在这个小验证集上找出最常见的错误模式。&lt;/li&gt;
&lt;li&gt;生成新特征，或添加更多训练数据，以修复观察到的错误模式。&lt;/li&gt;
&lt;li&gt;重复上述步骤，直到不再观察到频繁出现的错误模式。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;模型在部署前必须经过仔细评估，部署后也要持续评估。当模型最初训练完成时，基于历史数据进行离线模型评估。在线模型评估是指在生产环境中使用在线数据对模型进行测试和比较。两种流行的在线模型评估技术是 A/B 测试（A/B testing）和多臂老虎机（multi-armed bandit）。它们让我们能够确定新模型是否优于旧模型。&lt;/p&gt;
&lt;p&gt;模型可以按照以下几种模式部署：静态部署（作为可安装软件包的一部分）、在用户设备或服务器上动态部署，或通过模型流式传输（model streaming）部署。此外，还可以在单次部署（single deployment）、静默部署（silent deployment）、金丝雀部署（canary deployment）和多臂老虎机等策略中进行选择。每种模式和策略都有其优缺点，应根据你的业务应用来选择。&lt;/p&gt;
&lt;p&gt;算法效率也是模型部署的一个重要考虑因素。NumPy、SciPy 和 scikit-learn 等科学 Python 包是由经验丰富的科学家和工程师以效率为出发点构建的。它们有许多用 C 实现的方法，以实现最高效率。当你可以复用成熟流行的库或包时，避免自己编写生产代码。为了获得高效率，要选择合适的数据结构和缓存。&lt;/p&gt;</description></item></channel></rss>