<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>构建机器学习系统：使用特征存储 on Xuan</title><link>https://blog.430812.xyz/mlops/building-ml-systems/</link><description>Recent content in 构建机器学习系统：使用特征存储 on Xuan</description><generator>Hugo</generator><language>zh-CN</language><lastBuildDate>Sun, 09 Aug 2026 00:00:00 +0800</lastBuildDate><atom:link href="https://blog.430812.xyz/mlops/building-ml-systems/index.xml" rel="self" type="application/rss+xml"/><item><title>构建机器学习系统</title><link>https://blog.430812.xyz/mlops/building-ml-systems/01/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/01/</guid><description>&lt;h1 id="构建机器学习系统"&gt;构建机器学习系统&lt;/h1&gt;
&lt;p&gt;想象一下，你被指派为即将到来的财年制作一份财务预测。你决定使用机器学习（machine learning，ML），因为有大量可用数据，但不出所料，这些数据分散在许多不同的地方——电子表格里，以及数据仓库中许多不同的表里。你已经在同一家组织工作了几年，这不是你第一次接到这个任务。迄今为止的每一年，你模型的最终输出都是一份展示财务预测的 PowerPoint 演示文稿。每一年，你都训练一个新模型，你的模型只做一次预测，然后你就和它告别了。每一年，你几乎都是从零开始。你必须（再次）找到数据源，重新申请数据访问权限来为你的模型创建特征，然后翻出去年的 Jupyter notebook，用新数据和对模型的改进来更新它。&lt;/p&gt;
&lt;p&gt;然而今年，你意识到或许值得投入时间为此项目搭建脚手架，这样明年的工作量就会更少。因此，你决定不再交付 PowerPoint，而是构建一个仪表盘（dashboard）。你不再申请一次性的数据访问权限，而是构建特征流水线（feature pipeline），从数据源提取历史数据，并计算模型中使用的特征（和标签）。你有一个洞见：特征流水线可以做两件事：既计算用于训练模型的历史特征，也计算将作为已训练模型输入的特征，模型据此输出预测。现在，训练完模型后，你可以把它连接到特征流水线上做出预测，为你的仪表盘提供动力。当你只需要调整这个&lt;em&gt;ML 系统&lt;/em&gt;——增/改/删特征并训练新模型——时，你暗自庆幸。你毫不费力地把财务预测的频率更新为每季度一次。你把节省下来的时间——原本花在枯燥的数据获取、清洗和特征工程上——用来研究新的 ML 框架和模型架构，最终得到一个大幅改进的财务模型，这让你的老板非常高兴。&lt;/p&gt;
&lt;p&gt;这个例子展示了在静态数据集上训练模型做一次性预测，与构建&lt;em&gt;批处理 ML 系统&lt;/em&gt;（batch ML system）之间的区别——批处理 ML 系统是一个自动化系统，负责从数据源读取数据、把数据转换为特征、训练模型、用模型对新数据执行推理（inference），并用模型的预测更新仪表盘。仪表盘就是模型交付给利益相关者的价值。&lt;/p&gt;
&lt;p&gt;如果你想让模型产生重复价值，模型就应该不止一次地做预测。这意味着，当你在从静态数据集划分出的测试集上评估了模型性能之后，工作并没有结束。相反，你必须构建&lt;em&gt;ML 流水线&lt;/em&gt;（ML pipeline），即把原始数据转换为特征、把特征喂给模型以便轻松重训练、并把新特征喂给模型让它做出预测的程序——模型每做出一个新预测，就产生一份价值。&lt;/p&gt;
&lt;p&gt;通过这本书，你将踏上同样的旅程：从在静态数据集上训练模型，到构建&lt;em&gt;ML 系统&lt;/em&gt;——从决策树到深度学习，再到由大语言模型（LLM）驱动的智能体（agent）。这段旅程最重要的部分是处理动态数据。这意味着从静态数据（比如 &lt;a href="http://kaggle.com/"&gt;Kaggle.com&lt;/a&gt; 上 ML 竞赛中使用的人工整理数据集，以及为 LLM 编写提示词）出发，过渡到按一定间隔（每小时、每天、每周、每年）更新的批处理数据，再到构建智能交互应用所需的实时数据。&lt;/p&gt;
&lt;h1 id="机器学习系统的剖析"&gt;机器学习系统的剖析&lt;/h1&gt;
&lt;p&gt;构建 ML 系统时你将面临的主要挑战之一，是管理用于训练模型的数据和模型用来做预测的数据。我们可以根据 ML 系统如何处理用于预测的新数据来对它们分类。ML 系统是按计划做预测（例如每天一次），还是 24/7 运行、响应用户请求做预测？&lt;/p&gt;
&lt;p&gt;Spotify 的每周发现（Discovery Weekly）就是&lt;em&gt;批处理 ML 系统&lt;/em&gt;的一个例子：它是一个推荐引擎，每周预测一次你可能想听的歌曲并把它们加入你的播放列表。在批处理 ML 系统中，ML 系统读取一批数据（以 Spotify 为例，来自全部 575M+ 用户），并用训练好的推荐 ML 模型对这批数据中的所有行做预测。模型接收所有输入特征（比如你听音乐的频率和你听的音乐类型），为你预测出接下来一周的 30 首&amp;quot;最佳&amp;quot;歌曲。预测随后被存入数据库（Cassandra），当你登录时，Spotify 的每周推荐列表就从数据库下载下来，以推荐的形式显示在用户界面中。&lt;/p&gt;
&lt;p&gt;另一方面，TikTok 的推荐引擎以近乎实时地根据你的点击和观看调整其短视频推荐而闻名。TikTok 的推荐服务是一个&lt;em&gt;实时 ML 系统&lt;/em&gt;（real-time ML system）。它在你滑动和观看视频时预测该向你展示哪些视频。特斯拉前 AI 负责人 Andrej Karpathy &lt;a href="https://oreil.ly/jMiIX"&gt;曾说 TikTok 的推荐引擎&lt;/a&gt;&amp;ldquo;好得吓人。它就是数字毒品。&amp;ldquo;TikTok 是第一个包含实时推荐的在线视频平台，这给了它相对于老牌平台的竞争优势，使它得以建成全球第二受欢迎的在线视频平台。&lt;/p&gt;</description></item><item><title>机器学习管道</title><link>https://blog.430812.xyz/mlops/building-ml-systems/02/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/02/</guid><description>&lt;h1 id="机器学习管道"&gt;机器学习管道&lt;/h1&gt;
&lt;p&gt;在我最喜欢的《辛普森一家》（&lt;em&gt;The Simpsons&lt;/em&gt;）剧集中，当霍默·辛普森听说培根、火腿和猪排都来自同一种动物时，他简直不敢相信：「没错，丽莎，一只神奇而美妙的动物。」当我向 ChatGPT 4.1 询问 ML 管道（ML pipeline）的定义时，我也有同样的反应。它告诉我，ML 管道执行数据收集、特征工程、模型训练、模型评估、模型部署、模型监控、推理和维护。「没错，GPT，一条神奇而美妙的单体 ML 管道，」我想。它甚至声称它的 ML 管道是模块化的！&lt;/p&gt;
&lt;p&gt;难怪当我问 10 位不同的数据科学家什么是 ML 管道时，我通常会得到 10 种不同的答案。对于它的输入和输出是什么，人们没有达成共识。如果一位开发者告诉你，他们用 ML 管道构建了他们的 AI 系统，你能从中获得什么信息？在我看来，&lt;em&gt;ML pipeline&lt;em&gt;（ML 管道）这个术语，按照目前的使用方式，在讨论构建 AI 系统时可能「被认为是有害的」（considered harmful）。&lt;a href="ch02.html#ch01fn5"&gt;1&lt;/a&gt; 在本书中，我们力求更加严谨。我们用构建 AI 系统时所使用的具体管道来描述 AI 系统。我们保留 &lt;/em&gt;ML pipeline&lt;/em&gt; 这个术语的使用，用它来描述 AI 系统中的任何单个管道或一组管道。&lt;/p&gt;
&lt;p&gt;&lt;em&gt;管道&lt;em&gt;（pipeline）是一个具有明确定义的输入和输出的计算机程序（也就是说，它有一个定义良好的接口），并且按计划运行或持续运行。&lt;/em&gt;ML 管道&lt;em&gt;是任何输出用于 AI 系统的 ML 工件（ML artifact）的管道。我们以 ML 管道创建或修改的 ML 工件来命名具体的 ML 管道。创建 ML 工件的 ML 管道包括：输出特征的&lt;/em&gt;特征管道&lt;em&gt;（feature pipeline）、输出嵌入的&lt;/em&gt;向量嵌入管道&lt;em&gt;（vector-embedding pipeline）、输出已训练模型的&lt;/em&gt;训练管道&lt;em&gt;（training pipeline），以及输出预测的&lt;/em&gt;推理管道&lt;em&gt;（inference pipeline）。修改 ML 工件的 ML 管道包括：将模型从未验证状态转变为已验证状态的&lt;/em&gt;模型验证管道&lt;em&gt;（model validation pipeline），以及将模型部署到生产环境的&lt;/em&gt;模型部署管道&lt;/em&gt;（model deployment pipeline）。在本章中，我们介绍许多不同类型的 ML 管道，但我们将重点深入探讨对构建 AI 系统最重要的 ML 管道——特征管道、训练管道和推理管道。三条管道与真相。&lt;/p&gt;</description></item><item><title>你身边的空气质量预测服务</title><link>https://blog.430812.xyz/mlops/building-ml-systems/03/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/03/</guid><description>&lt;h1 id="你身边的空气质量预测服务"&gt;你身边的空气质量预测服务&lt;/h1&gt;
&lt;p&gt;我们要构建的第一个 ML 项目，是为一个你在意的社区提供空气质量预测服务。我们将遵循第 2 章的 MVPS 流程——&lt;em&gt;divide et impera&lt;/em&gt;（分而治之）。你的成果将是一项面向公众、旨在长久存续的服务，所以请在上面多花些时间和心思，你的社区会因此感谢你。我对这个项目有个人感情，因为我有两个患囊性纤维化（cystic fibrosis）的儿子——这是一种主要影响肺部的遗传性疾病。他们出生在同一天，相差两岁，也在同一天被确诊。总之，我认为我可以代表整个囊性纤维化社区说：对我们和许多人来说，这都会是一项很棒的服务！&lt;a href="ch03.html#ch01fn6"&gt;1&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;我们的 AI 系统要解决的预测问题是：预测你家、公司或任何地方附近的一个公共空气质量传感器所测得的空气质量。全球有一个由物联网（Internet of Things，IoT）爱好者组成的社区，他们把传感器装在自己的花园和阳台上，并把空气质量测量数据发布到互联网上。我居住的斯德哥尔摩有 30 多个公共传感器，而我的家乡都柏林有 40 多个。有一个&lt;a href="https://oreil.ly/K-Ppr"&gt;世界空气质量指数网站&lt;/a&gt;，你可以在它的地图上找到一个传感器，用来构建你的 AI 系统。选一个同时满足以下两个条件的传感器：(1) 有历史数据——我们要在历史数据上训练 ML 模型，所以如果你有几年数据，那就太好了；(2) 能产出可靠的测量值（有些传感器会在一段时间内关机或出现故障）。可靠的传感器能让你的 AI 系统持续收集测量数据，随着更多数据的积累，你就可以重新训练并改进模型。尽管你将为社区提供一项免费的公共服务，但你一分钱也不用花——我们将把系统运行在免费的 Serverless 服务（GitHub 和 Hopsworks）上。&lt;/p&gt;
&lt;p&gt;空气质量预测是一个相当直接的 ML 问题。我们把这个预测问题建模为回归（regression）问题——预测 PM 2.5 的值。PM 2.5 是一种细颗粒物指标，衡量直径小于等于 2.5 微米的颗粒物，其浓度过高会增加健康问题的风险，比如低出生体重、心脏病和肺病。PM 2.5 浓度过高还会降低能见度，使空气看起来灰蒙蒙的。我们将用什么特征来预测 PM 2.5 的水平呢？PM 2.5 与风速/风向、温度和降水相关，所以我们将使用天气预报数据来预测以 PM 2.5 衡量的空气质量。这很有道理，因为当风从特定方向吹来时，空气质量通常会更好——如果你住在繁忙的道路旁边，风向就至关重要。在较冷的天气里空气质量往往更差，因为冷空气比暖空气更稠密、移动更慢；而在城市里，通勤时开车的人可能比骑自行车的人更多。即使是印度那些冬季并不寒冷的地区，冬季月份的空气质量也更差。&lt;/p&gt;
&lt;p&gt;但是，等等。你可能读到过，空气质量预测已经是一个被解决的问题。2024 年，微软 AI 构建了 &lt;a href="https://oreil.ly/b2Xm5"&gt;Aurora&lt;/a&gt;，一个能预测全球空气污染的深度学习模型。与欧盟的&lt;a href="https://oreil.ly/IxZ5N"&gt;哥白尼计划&lt;/a&gt;在高性能计算基础设施上计算的空气物理模型相比，微软对 AI 的运用被誉为一大进步。然而，截至 2024 年年中，如果你在某个城市（比如斯德哥尔摩）检验 &lt;a href="https://oreil.ly/-IX8A"&gt;Aurora&lt;/a&gt; 的表现，你会发现它的预测与你可以在 &lt;a href="http://www.aqicn.org/"&gt;&lt;em&gt;aqicn.org&lt;/em&gt;&lt;/a&gt; 上找到的实际空气质量传感器读数相比并不准确。因此，你的挑战是构建一个 AI 系统，在你选择的空气质量传感器所在位置，用其成本的一小部分，就产生比 Aurora 更好的空气质量预测。在这个项目中，更高质量的数据加上决策树 ML 模型将胜过深度学习。&lt;/p&gt;
&lt;p&gt;最后，每个项目都会受益于一个&amp;quot;哇&amp;quot;的因素。我们将给项目撒上一些生成式 AI（GenAI）的魔法，通过一个由 LLM 驱动的语音 UI，让你的空气质量预测服务变得&amp;quot;友好&amp;quot;。&lt;/p&gt;</description></item><item><title>特征存储</title><link>https://blog.430812.xyz/mlops/building-ml-systems/04/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/04/</guid><description>&lt;h1 id="特征存储"&gt;特征存储&lt;/h1&gt;
&lt;p&gt;正如我们在前三章中所看到的，数据管理是构建和运维 AI 系统最具挑战性的方面之一。在上一章中，我们使用特征存储（feature store）构建了空气质量预测系统。特征存储存储了特征管道（feature pipeline）的输出，为训练管道提供训练数据，并为批推理管道提供推理数据。特征存储是一个核心数据平台，它为训练和推理存储、管理并提供特征。它还确保训练和推理中使用的特征之间的一致性，并通过提供共享数据层和定义良好的 API 来连接 FTI 管道，从而支持构建模块化的 AI 系统。&lt;/p&gt;
&lt;p&gt;在本章中，我们将更深入地探讨特征存储，并回答以下问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;特征存储解决了哪些问题，我什么时候需要它？&lt;/li&gt;
&lt;li&gt;什么是特征组（feature group），它如何存储数据，我如何向其中写入数据？&lt;/li&gt;
&lt;li&gt;如何为特征组设计数据模型？&lt;/li&gt;
&lt;li&gt;如何读取分布在许多特征组中的特征数据用于训练或推理？&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;我们将了解特征存储是如何由列式存储（columnar store）、行式存储（row-oriented store）和向量索引（vector index）构建的。我们将描述特征存储如何解决与特征复用相关的挑战、如何管理时序数据（time-series data），以及如何防止 FTI 管道之间的偏差（skew）。在本章中，我们还将穿插一个预测信用卡欺诈的实时 ML 系统作为激励性示例。&lt;/p&gt;
&lt;h1 id="用于欺诈预测的特征存储"&gt;用于欺诈预测的特征存储&lt;/h1&gt;
&lt;p&gt;我们首先介绍如何为一个对信用卡交易进行实时欺诈预测的 ML 系统设计特征存储。该系统的 ML 系统卡片如&lt;a href="#ml_system_card_for_our_real_time_credit"&gt;表 4-1&lt;/a&gt;所示。&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;动态数据源&lt;/th&gt;
					&lt;th&gt;预测问题&lt;/th&gt;
					&lt;th&gt;UI 或 API&lt;/th&gt;
					&lt;th&gt;监控&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;信用卡交易到达事件流平台。信用卡、发卡行和商户的详细信息以表的形式存储在数据仓库中。&lt;/td&gt;
					&lt;td&gt;信用卡交易是否被怀疑为欺诈&lt;/td&gt;
					&lt;td&gt;拒绝可疑欺诈交易的实时 API&lt;/td&gt;
					&lt;td&gt;对可疑欺诈与实际报告的欺诈进行离线调查&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;我们的 ML 系统的源数据来自一个数据集市（data mart），它由一个数据仓库和一个事件流平台（如 Apache Kafka 或 AWS Kinesis）组成（见&lt;a href="#we_design_our_feature_store_by_first_id"&gt;图 4-1&lt;/a&gt;）。&lt;/p&gt;
&lt;p&gt;该图展示了设计特征存储的过程，从数据集市中识别并创建特征开始，将它们组织成特征组，创建特征视图，并生成训练和推理数据。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/building-ml-systems/17_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;从数据源开始，我们将学习如何通过四个主要步骤构建特征存储：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;识别实体（entity）以及这些实体的特征。&lt;/li&gt;
&lt;li&gt;将实体组织成特征表（特征组），并识别特征组之间的关系。&lt;/li&gt;
&lt;li&gt;在特征视图（feature view）中，为模型选择可能来自不同特征组的特征。&lt;/li&gt;
&lt;li&gt;使用特征视图检索模型训练以及批/在线推理所需的数据。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;本章将更详细地介绍什么是特征组和特征视图，但在此之前，我们先来看看特征存储的历史、特征存储的组成（其解剖结构），以及何时可能需要特征存储。&lt;/p&gt;
&lt;h1 id="特征存储简史"&gt;特征存储简史&lt;/h1&gt;
&lt;p&gt;如&lt;a href="ch01.html#building_machine_learning_systems"&gt;第 1 章&lt;/a&gt;所述，Uber 作为其 Michelangelo 平台的一部分引入了第一个特征存储。Michelangelo 包含一个特征存储（称为 Palette）、一个模型注册表（model registry）和模型服务（model serving）能力。Michelangelo 还引入了一种领域特定语言（domain-specific language，DSL）来定义特征管道。在 DSL 中，你可以定义在什么数据源上计算什么类型的特征（例如，使用 &lt;code&gt;clicks&lt;/code&gt; 表计算用户在过去七天内的点击次数），Michelangelo 会把你的特征定义转译（transpile）成一个 Spark 程序，并按计划运行它（例如每小时或每天）。&lt;/p&gt;</description></item><item><title>Hopsworks 特征存储</title><link>https://blog.430812.xyz/mlops/building-ml-systems/05/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/05/</guid><description>&lt;h1 id="hopsworks-特征存储"&gt;Hopsworks 特征存储&lt;/h1&gt;
&lt;p&gt;在本章中，我们将深入探讨 Hopsworks 特征存储（feature store）。Hopsworks 是一个用于在规模上开发与运维批处理（batch）、实时（real-time）以及 LLM AI 系统的平台。它可以安装在一台服务器上，也可以安装在多达数百台服务器上。Hopsworks 包含一个特征存储，以及一个完整的 MLOps 与计算平台，但本章我们将聚焦于特征存储。我们将展示如何在 Hopsworks 中实现&lt;a href="ch04.html#feature_stores"&gt;第4章&lt;/a&gt;中信用卡欺诈模型的数据模型。我们还将通过 Python 代码片段，看到上一章介绍的特征存储概念在 Hopsworks 中是如何体现的。我们将从 Hopsworks 中的项目（project）开始——一个用于存储特征数据、训练数据和模型的安全协作空间。&lt;/p&gt;
&lt;h1 id="hopsworks-项目"&gt;Hopsworks 项目&lt;/h1&gt;
&lt;p&gt;Hopsworks 集群按项目（project）组织，每个项目都有唯一的名称。Hopsworks 项目是团队协作、管理 AI 数据和模型的安全空间。类似于 GitHub 中的仓库（repository），项目有团队成员（带基于角色的访问控制，role-based access control，RBAC），但与存储源代码不同，Hopsworks 项目存储的是用于 AI 的数据。每个项目都有自己的特征存储、模型注册表（model registry）、模型部署（model deployment），以及用于通用文件存储的数据集（dataset）。&lt;/p&gt;
&lt;p&gt;下面的代码片段展示了当你登录 Hopsworks 时如何获得一个项目对象的引用。如果你不输入项目名称，Hopsworks 将返回你的主项目（即你在&lt;a href="http://hopsworks.ai/"&gt;&lt;em&gt;hopsworks.ai&lt;/em&gt;&lt;/a&gt;注册账户时创建的项目）的引用。有了项目之后，你可以按如下方式获取其特征存储的引用：&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" style="background-color:#f8f8f8;-moz-tab-size:2;-o-tab-size:2;tab-size:2;-webkit-text-size-adjust:none;"&gt;&lt;code class="language-fallback" data-lang="fallback"&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;import hopsworks
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;project = hopsworks.login()
&lt;/span&gt;&lt;/span&gt;&lt;span style="display:flex;"&gt;&lt;span&gt;fs = project.get_feature_store()
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;&lt;code&gt;hopsworks.login()&lt;/code&gt; 方法还有用于 Hopsworks 集群的&lt;em&gt;主机名&lt;em&gt;（hostname）（或 IP）和端口的参数，以及 API 密钥（密钥值或包含 API 密钥的文件）。在本书中，我们将使用无服务器（serverless）Hopsworks，其主机名为 &lt;/em&gt;c.app.hopsworks.ai&lt;/em&gt;，端口为 443。在本书中，我们调用不带参数的 &lt;code&gt;hopsworks.login()&lt;/code&gt;，而是在你的程序中把 &lt;code&gt;HOPSWORKS_API_KEY&lt;/code&gt; 设置为环境变量。如果你不使用 Hopsworks serverless，你还需要设置 &lt;code&gt;HOPSWORKS_HOST&lt;/code&gt; 和 &lt;code&gt;HOPSWORKS_PROJECT&lt;/code&gt; 环境变量——把它们设置在本书源代码仓库根目录下的 &lt;em&gt;.env&lt;/em&gt; 文件中。&lt;/p&gt;</description></item><item><title>与模型无关的变换</title><link>https://blog.430812.xyz/mlops/building-ml-systems/06/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/06/</guid><description>&lt;h1 id="与模型无关的变换"&gt;与模型无关的变换&lt;/h1&gt;
&lt;p&gt;现在我们的焦点转向如何为特征管道编写数据变换逻辑。正如我们在&lt;a href="ch02.html#machine_learning_pipelines"&gt;第2章&lt;/a&gt;中解释的那样，特征管道（feature pipeline）是执行与模型无关的数据变换以产生可复用特征并将其存储在特征存储中的程序。也就是说，创建的特征数据可能被许多不同的模型使用——而不仅仅是你正在为其开发特征管道的第一个模型。特征复用通过增加使用量和测试来提高特征质量，降低存储成本，并降低特征开发和运维成本。请记住，成本最低的特征管道是你根本不需要创建的那条。&lt;/p&gt;
&lt;p&gt;与模型无关的变换（model-independent transformation，MIT）的例子包括提取、验证、聚合、压缩（EVAC）变换：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;特征&lt;em&gt;提取&lt;/em&gt;（滞后特征、分箱以及用于 LLM 的分块）&lt;/li&gt;
&lt;li&gt;数据&lt;em&gt;验证&lt;/em&gt;（使用 Great Expectations）与数据清洗&lt;/li&gt;
&lt;li&gt;&lt;em&gt;聚合&lt;/em&gt;（时间窗口内的计数与求和）&lt;/li&gt;
&lt;li&gt;&lt;em&gt;压缩&lt;/em&gt;（向量嵌入）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;我们还将探讨如何在特征管道中组合变换，以提高特征管道的模块化、可测试性和性能。不过，我们将从搭建开发流程开始——如何将源代码组织成包，以及可以使用哪些技术来实现特征管道中的变换。&lt;/p&gt;
&lt;h1 id="源代码组织"&gt;源代码组织&lt;/h1&gt;
&lt;p&gt;我们将使用信用卡欺诈项目的源代码作为模板，示范如何组织源代码，使其遵循开发 ML 管道的生产最佳实践。要构建生产级质量的管道，我们必须超越仅仅编写 notebook 的做法，这意味着要遵循软件工程实践，例如采用持续集成与持续部署（continuous integration and continuous deployment，CI/CD）的测试驱动开发。如果你修改了源代码，测试会给你更大的信心，相信你的改动不会破坏某条管道，也不会破坏依赖于你的管道所产出的工件（无论是特征、训练数据集、模型还是预测）的客户端。通过自动化执行测试，你在开发时不会拖慢迭代速度。如果你从未写过单元测试，别担心——LLM（如 ChatGPT）可以帮助你入门编写单元测试。&lt;/p&gt;
&lt;p&gt;我们采用一种目录结构，来组织构建、测试和运行整个信用卡欺诈预测系统所需的全部源代码（参见&lt;a href="#for_an_ai_system_built_with_pythoncomma"&gt;图 6-1&lt;/a&gt;）。&lt;/p&gt;
&lt;p&gt;图示：信用卡欺诈预测系统中用于组织源代码的目录结构，包含 notebooks、pipelines、features、tests 和 scripts 等独立目录。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/building-ml-systems/30_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;不同 FTI 管道的源代码存储在 &lt;em&gt;pipelines&lt;/em&gt; 目录中。为了便于维护，我们将&lt;em&gt;测试&lt;/em&gt;放在管道程序之外的专用目录中的独立文件里，这样就把管道的代码与测试的代码分开了。我们将有两类不同的测试：&lt;em&gt;特征测试&lt;em&gt;（feature test），即用于计算特征的单元测试；以及&lt;/em&gt;管道测试&lt;em&gt;（pipeline test），即针对管道的端到端测试。类似地，最好将用于计算特征的函数与实现 FTI 管道的程序分开。我们把特征函数放在 &lt;/em&gt;features&lt;/em&gt; 目录中。如果你遵循这种代码结构，你就能快速迭代，而不必在以后为生产环境重构代码。&lt;/p&gt;
&lt;p&gt;我们把这种项目结构称为&lt;em&gt;单体仓库&lt;em&gt;（monorepo），因为我们整个 AI 系统的源代码都位于单个源代码仓库中。与为 FTI 管道分别建仓库相比，单体仓库的优势在于，我们不必为 ML 管道之间的任何共享代码创建和管理可安装的 Python 库。单体仓库也不会妨碍为 FTI 管道创建独立的生产级部署。例如，每条 ML 管道都可以在自己的目录中拥有自己的 &lt;/em&gt;requirements.txt&lt;/em&gt; 文件，用于为 ML 管道构建可执行的容器镜像。&lt;/p&gt;
&lt;p&gt;请注意，&lt;em&gt;notebooks&lt;em&gt; 是一个独立的目录。它通常不属于项目中的生产代码。它的存在是为了产生创建生产代码所需的洞见——执行 EDA（探索性数据分析，exploratory data analysis）以理解数据和预测问题，并将这些洞见传达给其他利益相关者。话虽如此，在一些平台（如 Hopsworks 和 Databricks）上，你可以将 notebook 作为作业运行，所以如果你真的想，也可以把特征管道、训练管道和批量推理管道作为作业来运行。&lt;/em&gt;scripts&lt;/em&gt; 目录不是生产代码，它用于存放开发期间对管道运行测试的实用 shell 脚本。&lt;/p&gt;</description></item><item><title>与模型相关的变换和按需变换</title><link>https://blog.430812.xyz/mlops/building-ml-systems/07/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/07/</guid><description>&lt;h1 id="与模型相关的变换和按需变换"&gt;与模型相关的变换和按需变换&lt;/h1&gt;
&lt;p&gt;在本章中，我们将研究训练与推理管道中的数据变换，以及如何确保两条管道中的变换是等价的。我们在&lt;a href="ch02.html#machine_learning_pipelines"&gt;第2章&lt;/a&gt;中介绍过，与模型相关的变换（model-dependent transformation，MDT）是在数据从特征存储读出之后执行的、为某个特定模型创建特征的数据变换。MDT 有两大类——特征变换（针对数值特征和类别特征），以及仅与一个模型紧密耦合的变换。前者的例子是对类别变量进行独热编码，后者的例子是面向 LLM 的文本编码。&lt;/p&gt;
&lt;p&gt;我们还将研究如何防止在训练和推理管道中分别应用的 MDT 之间出现&lt;em&gt;偏差&lt;em&gt;（skew）。这并不总是像在训练和推理管道中应用同一个带版本函数那样简单，因为许多 MDT 是有状态的，需要把相同的状态（模型的训练数据统计量）作为参数同时传入训练和推理管道。我们首先介绍特征变换的常见示例以及不同类别的模型特定变换。然后，我们研究防止偏差的不同机制，包括 Scikit-Learn 管道、PyTorch 变换，以及 Hopsworks 特征视图中的变换函数。我们还将介绍最后一类数据变换——按需变换（on-demand transformation，ODT），它存在于在线推理管道和特征管道中，通常是无状态的变换函数。最后，我们用 &lt;/em&gt;pytest&lt;/em&gt; 对变换函数进行单元测试来结束本章。&lt;/p&gt;
&lt;h1 id="特征变换"&gt;特征变换&lt;/h1&gt;
&lt;p&gt;特征变换可以提升各类 ML 模型的性能和收敛性。例如，大多数 ML 算法不能接受字符串作为输入，需要将其变换为数值格式。ML 模型的最终输入通常是一个数值数组。类似地，深度学习模型通常要求数值特征被归一化或变换为服从正态分布，以确保正确收敛。&lt;/p&gt;
&lt;p&gt;不同的特征变换针对特定的特征类型（类别型或数值型）执行。特征类型有助于确定哪种特征变换是合适的。例如，编码（encoding）用于将类别变量转换为数值格式，而缩放（scaling）用于调整数值变量的范围或分布。这些变换通常以训练数据的属性为参数，例如类别集合或描述性统计量（最小值、最大值、均值、标准差或众数）。例如，当你对类别变量进行独热编码时，必须首先枚举训练数据中的所有类别，然后才能将该字符串编码为二元向量。类似地，当对数值变量应用标准化（standardization，也称为 &lt;em&gt;z 分数归一化&lt;/em&gt;）时，必须先从训练数据中计算出均值和标准差，然后用它们一致地缩放数据集中的所有特征值。&lt;/p&gt;
&lt;h2 id="编码类别变量"&gt;编码类别变量&lt;/h2&gt;
&lt;p&gt;在特征编码算法中，类别集合可能随时间变化，为了处理这一点，你应该包含一个特殊类别（称为 &amp;ldquo;unknown&amp;rdquo; 或 &amp;ldquo;other&amp;rdquo;），用于推理期间出现的任何新类别。例如，信用卡支付给出的商户类别代码（merchant category code）对许多奖励计划很重要，这些计划为特定类型的消费（如旅行）提供积分。每个商户通常有一个添加到信用卡支付中的单一类别。在&lt;a href="#one_hot_encoding_of_the_merchant_catego"&gt;表 7-1&lt;/a&gt;中，我们对类别进行了独热编码。为简单起见，我只展示四个类别，而实际上有数百个。每个独热编码数组表示一个类别，在数组的该类别位置为 &lt;em&gt;1&lt;/em&gt;，在其他所有位置为 &lt;em&gt;0&lt;/em&gt;。&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;商户类别&lt;/th&gt;
					&lt;th&gt;独热编码&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;航空公司&lt;/td&gt;
					&lt;td&gt;[1,0,0,0]&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;餐饮场所和餐厅&lt;/td&gt;
					&lt;td&gt;[0,1,0,0]&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;汽车租赁&lt;/td&gt;
					&lt;td&gt;[0,0,1,0]&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;酒店、汽车旅馆和度假村&lt;/td&gt;
					&lt;td&gt;[0,0,0,1]&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;当存在&lt;em&gt;高基数&lt;/em&gt;（high cardinality，即类别数量很大）时，不建议使用独热编码，因为每个类别都会增加一个新维度，从而增加内存使用量。当类别之间存在序数关系时，它也不适用，因为它不保留顺序，如&lt;a href="#popular_algorithms_for_encoding_categor"&gt;表 7-2&lt;/a&gt;所示。&lt;/p&gt;
&lt;p&gt;如果变量之间存在序数关系，那么&lt;em&gt;序数编码器&lt;/em&gt;（ordinal encoder）会在变换后的类别中保留顺序。&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;算法&lt;/th&gt;
					&lt;th&gt;用途&lt;/th&gt;
					&lt;th&gt;使用场景&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;独热编码器&lt;/td&gt;
					&lt;td&gt;将类别数据变换为独热编码向量（一个字节数组，每个类别代表一位）&lt;/td&gt;
					&lt;td&gt;在不存在序数关系且基数较低到中等时，变换为独热编码&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;序数编码器&lt;/td&gt;
					&lt;td&gt;将类别数据变换为一个整数&lt;/td&gt;
					&lt;td&gt;对具有序数关系的特征进行编码&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;特征哈希器&lt;/td&gt;
					&lt;td&gt;使用哈希技巧将类别数据变换为固定大小的向量&lt;/td&gt;
					&lt;td&gt;具有许多唯一类别的高维数据&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;标签编码器&lt;/td&gt;
					&lt;td&gt;用 0 到 n_classes-1 之间的值对目标标签进行编码&lt;/td&gt;
					&lt;td&gt;对目标/标签变量进行编码&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;对于具有大量类别的特征，特征哈希（&lt;em&gt;特征哈希器&lt;em&gt;编码算法）通过将类别映射到固定大小的哈希表来降低维度，尽管这引入了哈希冲突（hash collision，即不同类别映射到相同值）的风险。如果你使用特征哈希器，请确保你的 ML 算法能够容忍可能的哈希冲突。最后，&lt;/em&gt;标签编码&lt;/em&gt;（label encoding）通常用于将目标/标签变量编码为整数，从而保留顺序。许多 ML 算法（如 Scikit-Learn 的逻辑回归和 XGBoost 的多类分类）要求标签（目标变量）为整数编码。&lt;/p&gt;</description></item><item><title>批处理特征管道</title><link>https://blog.430812.xyz/mlops/building-ml-systems/08/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/08/</guid><description>&lt;h1 id="批处理特征管道"&gt;批处理特征管道&lt;/h1&gt;
&lt;p&gt;在前两章中，我们探讨了如何实现数据变换来创建可复用特征和模型专属特征。现在，我们将研究如何使用&lt;em&gt;批处理特征管道&lt;em&gt;（batch feature pipeline）将可复用特征数据的创建过程生产化。批处理特征管道是一个程序，它从数据源读取数据，对提取的数据应用 MIT，并将计算出的特征数据存储在特征存储中。批处理特征管道可以按计划运行，例如每小时或每天运行一次，增量处理新到达且可供处理的数据。它也可以按需运行，将大量历史数据变换为特征，这一过程被称为&lt;/em&gt;回填&lt;/em&gt;（backfilling）。&lt;/p&gt;
&lt;p&gt;批处理特征管道的目标是在所谓的&lt;em&gt;批处理&lt;/em&gt;（batch processing）中自动化特征创建，与一次处理单条记录相比，批处理在资源利用上更高效。例如，想象一下比较一次一个玻璃杯或一个盘子地清空洗碗机与成批卸下盘子和玻璃杯所需的时间。类似地，在数据处理中，成批处理数据比一次处理一条记录要高效得多。此外，如果每天执行批处理，你可以利用夜间成本更低的非高峰处理时间。与流处理相比，另一个运维上的好处是，错误只需要在下一次计划运行的批处理特征管道之前修复——你可能不需要被寻呼机叫醒来修复管道。批处理的缺点是，你的特征数据的新鲜度只能保证到批处理运行的时间间隔。&lt;/p&gt;
&lt;p&gt;在本章中，你还将学习如何通过提示 LLM 创建一个生成合成数据的程序，为我们的信用卡欺诈数据集市创建合成数据。你还将学习如何编写一个可以针对数据源进行参数化的批处理特征管道，使其能够在回填或生产（增量数据处理）模式下运行。我们将介绍用于运行批处理特征管道的编排器（orchestrator）。最后，你将学习如何通过提供数据质量保证来为特征组设计&lt;em&gt;数据契约&lt;/em&gt;（data contract）。这将涉及在特征数据存储到特征存储之前，使用 Great Expectations 验证特征数据，并使用特征组的模式化标签（schematized tag）执行数据治理检查。&lt;/p&gt;
&lt;h1 id="批处理特征管道-1"&gt;批处理特征管道&lt;/h1&gt;
&lt;p&gt;特征管道是一种&lt;em&gt;数据管道&lt;em&gt;（data pipeline）——一个自动化地将数据从一个或多个数据源传输和变换到目标数据存储（称为&lt;/em&gt;数据汇&lt;em&gt;（data sink））的程序。在&lt;a href="ch04.html#feature_stores"&gt;第4章&lt;/a&gt;中，我们介绍了数据管道的两个流行类别：ETL 和 ELT 管道。ETL 管道在数据写入目标之前进行变换，而 ELT 管道先将数据写入目标，然后就地变换数据（通常使用数据仓库中的 SQL）。数据管道是需要按计划运行（称为&lt;/em&gt;批处理数据管道&lt;em&gt;（batch data pipeline））或全天候运行（称为&lt;/em&gt;流式数据管道&lt;/em&gt;（streaming data pipeline））的运维服务。批处理特征管道是将源数据变换为特征数据的批处理数据管道，通常将其输出存储在特征存储中。&lt;/p&gt;
&lt;p&gt;批处理特征管道可以实现为 ELT 或 ETL 管道，但最常见的是 ETL 管道。ELT 管道是 SQL 程序，它们高效且易于创建聚合、统计特征和滞后特征等流行特征。然而，SQL 在特征工程能力上有限，大多数批处理特征管道是 ETL 程序。作为 ETL 程序的批处理特征管道通常是 Python 程序（Pandas、Polars、PySpark），并通过利用 Python 生态系统的数据变换库来支持更丰富的特征创建能力。例如，有用于创建向量嵌入、网页抓取、从第三方 API 读取数据以及轻松集成 LLM API 进行数据处理和信息检索的 Python 库。&lt;/p&gt;
&lt;p&gt;作为 ETL 程序的批处理特征管道具有共同的结构：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;程序的一次执行运行由编排器调度或触发。&lt;/li&gt;
&lt;li&gt;从一个或多个数据源读取输入数据，并带有本次运行要处理的输入数据时间范围的开始/结束时间戳。&lt;/li&gt;
&lt;li&gt;一个由 MIT 组成的有向无环图（DAG）为特征组创建特征数据。&lt;/li&gt;
&lt;li&gt;对特征数据应用一组数据和模式验证检查。&lt;/li&gt;
&lt;li&gt;特征数据被保存到一个或多个特征组。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;我们将从研究特征管道（批处理和流式）的不同类型数据源开始。&lt;/p&gt;
&lt;h1 id="特征管道数据源"&gt;特征管道数据源&lt;/h1&gt;
&lt;p&gt;AI 系统的数据源头由连接到用户、机器和现实世界的应用程序、服务和设备组成。它们产生的数据存储在运营数据库、湖仓或数据仓库（基于对象存储）以及事件流平台中。这些数据存储是特征管道的主要数据源，它们属于三类之一：批处理数据源、（事件）流数据源和 API 数据源（参见&lt;a href="#simplified_architecture_of_data_stores"&gt;图 8-1&lt;/a&gt;）。&lt;/p&gt;</description></item><item><title>流式与实时特征</title><link>https://blog.430812.xyz/mlops/building-ml-systems/09/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/09/</guid><description>&lt;h1 id="流式与实时特征"&gt;流式与实时特征&lt;/h1&gt;
&lt;p&gt;如果你想构建一个可扩展的实时机器学习系统，其特征新鲜度（feature freshness）只有几秒钟，那么你需要流式特征管道。&lt;em&gt;流式特征管道（streaming feature pipeline）&lt;/em&gt; 是一个 24/7 全天候运行的流处理程序，它从流式数据源消费事件，可能还会用其他数据源的数据来丰富这些事件，应用数据变换来创建特征，并将输出的特征数据写入特征存储（feature store）。&lt;/p&gt;
&lt;p&gt;在运维层面，流式管道与微服务（microservice）的共同点比与批处理管道的共同点多。如果流式管道发生故障，往往需要立即修复，你不能等到下一个计划中的批处理运行再来修复。流处理程序会将无限的事件流划分（分区）为相关事件的分组，这些分组在窗口（window）中一起被处理。&lt;em&gt;窗口&lt;/em&gt; 是一组受时间约束的事件。例如，流式管道可以创建一个窗口，按信用卡号对过去一小时的信用卡交易进行分组，并基于这些事件计算特征，比如每张卡在过去一小时的交易次数。在这种情况下，你需要考虑如何处理在其处理窗口关闭之后才姗姗来迟的数据。例如，一笔迟到了两个小时的信用卡交易应该怎么处理？尽管存在这些挑战，流式特征管道正越来越多地被用于构建实时机器学习系统。随着流处理框架现在支持 SQL 和 Python，以及 Java 等传统语言，它们对开发者来说也变得越来越容易上手。&lt;/p&gt;
&lt;p&gt;但流处理并不总是实时特征所必需的。有时，捕获世界最近事件信息的新鲜特征——例如用户在过去 30 秒内点击按钮的次数——可以在在线推理管道中利用原始事件数据作为 ODT 来计算。我们将首先探讨实时特征对于构建能够实时智能地响应用户输入和环境变化的交互式 AI 系统是多么关键。&lt;/p&gt;
&lt;h1 id="交互式-ai-系统需要实时特征"&gt;交互式 AI 系统需要实时特征&lt;/h1&gt;
&lt;p&gt;交互式 AI 系统会根据上下文、用户操作和环境变化实时调整其行为。交互式 AI 系统可以建立在经典机器学习模型、深度学习模型或 LLM 之上。在&lt;a href="ch01.html#building_machine_learning_systems"&gt;第1章&lt;/a&gt;中，我们以 TikTok 为例介绍了交互式 AI 系统：它利用 AI 根据用户最近的操作和上下文来推荐视频。TikTok 的制造商字节跳动构建了庞大的实时数据处理基础设施，以确保其 AI 感觉响应迅速而不卡顿。TikTok 的推荐系统借助经典机器学习模型和深度学习模型，能在一秒左右内适应你的非语言操作（滑动、点赞、搜索）。&lt;/p&gt;
&lt;p&gt;交互式应用还可以利用智能体（agent）和基于 LLM 的应用（见&lt;a href="ch12.html#agents_and_llm_workflows"&gt;第12章&lt;/a&gt;）成为实时 AI——方法是将智能体的 API 扩展为除了用户提示词之外还包含 ID。应用会使用许多 ID 来跟踪用户、用户操作、点击流和应用状态（订单、文章、交易等）。当应用向智能体或 LLM 应用发出查询时，它还可以将应用 ID 作为查询上下文的一部分包含在内。例如，如果用户问&amp;quot;我上周订购的鞋子怎么了？&amp;quot;，智能体将收到该查询以及用户 ID。然后可以用该用户 ID 从特征存储中检索上一周与该用户相关的所有事件。这些事件可以连同用户查询一起作为上下文传入系统提示词，使 LLM 能够综合出正确的答案：鞋子已于昨天发货。实际上，我们可以将在线特征存储用作智能体和 LLM 的 RAG 检索引擎（见图 9-1）。&lt;/p&gt;
&lt;p&gt;描述交互式 AI 应用如何使用 ID 从智能体和在线特征存储中查询与检索信息的示意图，该架构通过流式数据集成支持实时数据处理。&lt;/p&gt;</description></item><item><title>训练管道</title><link>https://blog.430812.xyz/mlops/building-ml-systems/10/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/10/</guid><description>&lt;h1 id="训练管道"&gt;训练管道&lt;/h1&gt;
&lt;p&gt;模型训练是数据科学中最广泛、最深入的领域。我们将涵盖训练全谱系模型时所涉及的最重要概念和可扩展性挑战，从 XGBoost 决策树，到使用 Ray 进行大规模深度学习，再到使用低秩适配（low-rank adaptation, LoRA）微调 LLM。关于这些主题有大量资源可供深入学习。我们将聚焦于掌握模型训练的阴阳两面：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;em&gt;&lt;em&gt;以模型为中心的 AI（model-centric AI）&lt;/em&gt;&lt;/em&gt;
&lt;ul&gt;
&lt;li&gt;通过实验模型架构和调优超参数来迭代改进模型性能的过程&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;em&gt;&lt;em&gt;以数据为中心的 AI（data-centric AI）&lt;/em&gt;&lt;/em&gt;
&lt;ul&gt;
&lt;li&gt;通过选择特征和数据来迭代改进模型性能的过程&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;要成为一名出色的数据科学家，你需要同时擅长以模型为中心和以数据为中心的训练。秉持我们的阴阳哲学，我们将涵盖训练管道（training pipeline）最重要的实用要素：学习算法的选择、在特征存储（feature store）中将标签连接到特征、特征选择（feature selection）、训练数据集创建、模型架构、分布式训练（distributed training）和模型评估。我们还将审视在 GPU 上扩展模型训练时所面临的性能挑战。&lt;/p&gt;
&lt;h1 id="特征组中的非结构化数据与标签"&gt;特征组中的非结构化数据与标签&lt;/h1&gt;
&lt;p&gt;在&lt;a href="ch02.html#machine_learning_pipelines"&gt;第2章&lt;/a&gt;的 MVPS 开发方法论中，你从识别预测问题和可用于解决该问题的数据源开始。预测问题可分为三类：需要数据集中显式标签/目标的监督学习（supervised learning）、不需要带标签数据的无监督学习（unsupervised learning），以及从数据中自行创建标签的自监督学习（self-supervised learning）。自监督学习和无监督学习传统上与图像、音频、视频和文本文件等非结构化数据相关联。&lt;/p&gt;
&lt;h2 id="自监督学习与无监督学习"&gt;自监督学习与无监督学习&lt;/h2&gt;
&lt;p&gt;自监督学习和无监督学习模型不需要单独的标签/目标。例如，LLM 预测一段文本序列中的下一个 token。你不需要外部提供的标签，因为标签就是下一个 token。自监督学习是指算法从输入数据中自动生成标签。LLM 使用预测出的下一个 token 来预测后续 token，并不断使用之前的 token 预测来继续预测，直到预测出一个停止 token。使用先前预测作为输入的模型被称为&lt;em&gt;自回归模型（autoregressive model）&lt;/em&gt;。&lt;/p&gt;
&lt;h6 id="注意"&gt;注意&lt;/h6&gt;
&lt;p&gt;自回归模型可能不稳定。例如，在我们的空气质量示例中，如果仅使用滞后的空气质量（例如，前一二三天的数据）作为特征来提前七天预测空气质量，预测中可能会出现误差累积，产生失控的预测。解决方案是使用天气特征来稳定预测，这使得滞后空气质量成为一个好特征，只要你不让模型在滞后特征上过拟合。&lt;/p&gt;
&lt;p&gt;另一种用于语言模型的自监督算法是&lt;em&gt;掩码语言建模（masked language modeling）&lt;/em&gt;，它因 BERT 变换器模型而广为人知。在训练期间，BERT 随机掩码（隐藏）输入文本序列中的目标词，并训练语言模型预测缺失的词。由于 BERT 不使用先前预测的词，它不是自回归的。&lt;/p&gt;
&lt;p&gt;特征存储可以通过在特征组中索引关于非结构化数据文件的信息来管理非结构化数据（用于无监督和自监督 ML），从而更容易处理和搜索文件。对于非结构化数据集中的每个文件，你在特征组中存储一行，包含关于该文件的元数据和文件路径。&lt;a href="#self_supervised_and_unsupervised_data_i"&gt;表 10-1&lt;/a&gt; 展示了一些无监督和自监督 ML 模型的示例，以及为它们存储在特征组中的数据。&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;ML 模型&lt;/th&gt;
					&lt;th&gt;特征组&lt;/th&gt;
					&lt;th&gt;预测问题&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;预训练 LLM&lt;/td&gt;
					&lt;td&gt;存储用作训练数据的文件名和元数据的特征组&lt;/td&gt;
					&lt;td&gt;预测下一个 token。自监督。&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;向量嵌入（vector embeddings）&lt;/td&gt;
					&lt;td&gt;存储图像、音频和文本文件特征和嵌入的特征组&lt;/td&gt;
					&lt;td&gt;ANN 搜索。无监督。&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;kNN&lt;/td&gt;
					&lt;td&gt;存储图像、音频和文本文件特征的特征组&lt;/td&gt;
					&lt;td&gt;搜索/聚类。无监督。&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;GAN&lt;/td&gt;
					&lt;td&gt;存储图像、音频和文本文件文件名和元数据的特征组&lt;/td&gt;
					&lt;td&gt;异常检测。无监督。&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;稳定扩散（stable diffusion）&lt;/td&gt;
					&lt;td&gt;存储图像文件名、元数据和图像文本描述的特征组&lt;/td&gt;
					&lt;td&gt;从文本描述生成图像。部分无监督。&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;k 近邻（k-nearest neighbor, kNN）是一种无监督学习算法，用于 (a) 近似最近邻（approximate nearest neighbor, ANN）搜索和 (b) 对一组无标签数据点进行分割或聚类。你也可以将 kNN 用作分类/回归的监督方法。&lt;/p&gt;</description></item><item><title>推理管道</title><link>https://blog.430812.xyz/mlops/building-ml-systems/11/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/11/</guid><description>&lt;h1 id="推理管道"&gt;推理管道&lt;/h1&gt;
&lt;p&gt;推理管道（inference pipeline）决定了你所构建的 AI 系统的类型。批推理管道（batch inference pipeline）是批式 AI 系统，在线推理管道（online inference pipeline）是实时 AI 系统，而智能体工作流（agentic workflow）则是基于 LLM 的 AI 系统。&lt;em&gt;推理管道&lt;/em&gt; 是一个程序，它获取推理数据，对输入数据应用变换以产生一个或多个特征向量（feature vector），然后将这些特征向量馈送给一个或多个输出预测的模型。推理管道可以是批处理/流式/嵌入式程序，也可以是带 SLO 的网络服务，还可以是使用 LLM 和工具来实现目标的智能体。推理管道会记录其输入和输出，以便你能够监控和调试它们的性能。&lt;/p&gt;
&lt;p&gt;本章涵盖编写批式、在线、嵌入式（embedded）和流式（streaming）推理程序时面临的挑战。智能体和 LLM 工作流将在&lt;a href="ch12.html#agents_and_llm_workflows"&gt;第12章&lt;/a&gt;中介绍。你将学习如何设计批推理管道，并使用 PySpark 将其横向扩展。你将学习如何编写在线推理管道，从特征存储（feature store）中检索上下文/历史信息，以及如何在部署 API 背后的模型服务基础设施中部署模型。你还将学习如何将模型嵌入到流处理应用程序中，以及如何用 Python 为你的 AI 系统编写用户界面。&lt;/p&gt;
&lt;h1 id="批推理管道"&gt;批推理管道&lt;/h1&gt;
&lt;p&gt;批推理管道进行非时间关键的预测，按计划运行，并将预测输出到某种推理存储（inference store），消费者从中异步检索他们的预测。它们通常通过查询特征存储来获取推理数据。例如，在&lt;a href="ch03.html#your_friendly_neighborhood_air_quality"&gt;第3章&lt;/a&gt;的空气质量系统中，我们日常的批推理管道从特征存储中读取天气预报数据，做出空气质量预测，并将预测/特征记录到特征存储中。&lt;em&gt;推理存储&lt;/em&gt; 是任何存储批推理管道预测结果的数据存储。它可以是任何东西，从数据库到特征存储、对象存储，或事件流平台。你的批推理管道不一定非要写入推理存储——空气质量系统本可以只发布其仪表盘，而不写入预测（也不发布后报（hindcast））。但在生产系统中，你的仪表盘通常是根据推理存储中的预测创建的，而运营系统（如&lt;a href="ch01.html#building_machine_learning_systems"&gt;第1章&lt;/a&gt;中的 Spotify 每周发现（Discovery Weekly）示例）和监控系统（我们空气质量系统的后报）也会消费推理存储中的预测。&lt;/p&gt;
&lt;p&gt;一个典型的批推理管道执行以下步骤：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;使用特征视图（feature view）从湖仓表（lakehouse table）中读取/查询预计算的推理（预计算特征）数据。&lt;/li&gt;
&lt;li&gt;对推理数据应用 MDT（模型相关变换（model-dependent transformation，MDT））。&lt;/li&gt;
&lt;li&gt;使用变换后的推理数据调用 &lt;code&gt;model.predict(..)&lt;/code&gt;。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;推理数据是用于做出预测的特征数据。你如何查询推理数据取决于你正在解决什么类型的批推理问题。在接下来的几节中，我们将描述做出以下预测的批推理管道：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;基于一段时间范围内的推理数据（例如昨天到达的数据或未来七天的预报）&lt;/li&gt;
&lt;li&gt;针对实体的预测，例如对所有客户的预测或对所有有库存产品的预测&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;我们还将研究如何使用 PySpark 横向扩展批推理管道，以及如何重构你的数据模型，以在写入湖仓表时提高性能。&lt;/p&gt;
&lt;h2 id="针对时间范围的批预测"&gt;针对时间范围的批预测&lt;/h2&gt;
&lt;p&gt;&lt;a href="#with_a_feature_viewcomma_you_can_read_a"&gt;图 11-1&lt;/a&gt; 展示了如何使用特征视图同时检索训练数据和特定时间范围的批量推理数据。每批数据都使用&lt;em&gt;查询（query）&lt;/em&gt;读取（见&lt;a href="ch05.html#hopsworks_feature_store"&gt;第5章&lt;/a&gt;）。在 v1 中，查询包含训练数据的开始和结束时间。在 v2 中，查询还包含一个过滤器，用于筛选 &lt;code&gt;country&lt;/code&gt; 为 &lt;code&gt;US&lt;/code&gt; 的数据。请注意，如果你只用 &lt;code&gt;US&lt;/code&gt; 的数据训练模型，那么你的推理数据也应该只检索 &lt;code&gt;US&lt;/code&gt; 的数据。训练和推理中应使用相同的过滤器。这既适用于批推理，也适用于在线推理。&lt;/p&gt;</description></item><item><title>智能体与 LLM 工作流</title><link>https://blog.430812.xyz/mlops/building-ml-systems/12/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/12/</guid><description>&lt;h1 id="智能体与-llm-工作流"&gt;智能体与 LLM 工作流&lt;/h1&gt;
&lt;p&gt;LLM 工作流（LLM workflow）和智能体（agent）很容易识别，因为它们是提供自然语言 API 的 AI 驱动服务。第一批基于 LLM 的聊天机器人只是 LLM 的简单封装（wrapper）。但它们无法回答任何发生在训练截止日期（training cutoff date）之后的事件的问题。因此，它们迅速演变成复杂的多步骤引擎，能够回答甚至关于今天发生的事件的问题——利用向量索引（vector index）、搜索引擎、特征存储（feature store）和其他数据源向提示词（prompt）添加上下文信息。&lt;/p&gt;
&lt;p&gt;借助工具和新协议，LLM 工作流已经变形（transmogrify）为智能体，智能体在如何规划和执行任务以实现目标方面拥有一定程度的自主性。智能体不仅仅是 LLM 的封装。它们可以使用外部工具，拥有记忆（memory），并且能够规划实现目标的策略。智能体大多是交互式服务，但也存在&lt;em&gt;后台智能体（background agent）&lt;/em&gt;，它们自主执行任务，自动化日常工作，如工作流执行、流程优化和主动维护。&lt;/p&gt;
&lt;p&gt;在本章中，我们将深入探索构建 LLM 工作流和智能体的兔子洞。我们将学习上下文工程（context engineering）的艺术，为每次与 LLM 的交互提供尽可能多的上下文和先验知识。为此，你可能需要查询各种数据源（向量索引、搜索引擎、特征存储等）、调用外部 API，甚至使用其他智能体。我们还将介绍两种协议——模型上下文协议（Model Context Protocol，MCP）和智能体到智能体（Agent-to-Agent，A2A）——它们分别标准化了对各种工具和智能体的访问。标准化协议使智能体能够在运行时发现和使用工具及其他智能体——当前 LLM 的一个挑战是它们有限的规划能力。我们还将研究 LLM 工作流模式，例如路由（routing），以约束赋予智能体的自主性，确保它们能产出有用的结果。最后，由于智能体是软件组件，我们将研究一种迭代开发和部署智能体的软件开发流程。我们将在&lt;a href="ch13.html#testing_ai_systems"&gt;第13章&lt;/a&gt;和&lt;a href="ch14.html#observability_and_monitoring_ai_systems"&gt;第14章&lt;/a&gt;中介绍智能体的测试和监控。&lt;/p&gt;
&lt;h1 id="从-llm-到智能体"&gt;从 LLM 到智能体&lt;/h1&gt;
&lt;p&gt;第一批使用 LLM 的聊天机器人将用户查询与聊天机器人的&lt;em&gt;系统提示词（system prompt）&lt;/em&gt;结合在一起。系统提示词通过诸如&amp;quot;做一个乐于助人的聊天助手，不要作恶&amp;quot;之类的话语，帮助回复遵循预期的准则。组合后的系统提示词和用户查询被发送给 LLM，LLM 的回复则输出给客户端。&lt;/p&gt;
&lt;p&gt;很快人们就发现，LLM 无法回答任何发生在训练截止时间之后的事件的问题。例如，在 2025 年 7 月，如果我询问谁赢得了 2025 年的 NBA 冠军，LLM 将无法正确回答。检索增强生成（retrieval-augmented generation，RAG）被引入，作为一种在查询时动态地将检索到的示例添加到系统提示词的方法。最早的 RAG 实现使用用户查询从向量索引中检索相似的文本块。图 12-1 展示了带向量索引的 LLM RAG 架构。&lt;/p&gt;
&lt;p&gt;图示说明检索增强生成（RAG）架构，展示查询如何经过向量索引和提示模板处理，再经由语言模型（LLM）生成响应。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/building-ml-systems/72_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;要让 RAG 工作，你需要定期用新数据更新向量索引。向量嵌入管道（vector-embedding pipeline）用文本更新向量索引，文本首先被分块（chunking），然后用嵌入模型（embedding model）编码：&lt;/p&gt;</description></item><item><title>测试 AI 系统</title><link>https://blog.430812.xyz/mlops/building-ml-systems/13/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/13/</guid><description>&lt;h1 id="测试-ai-系统"&gt;测试 AI 系统&lt;/h1&gt;
&lt;p&gt;MLOps 是一套最佳实践，用于对驱动我们 AI 系统的 ML 管道和 ML 资产进行自动化测试、版本控制和监控。我们在&lt;a href="ch01.html#building_machine_learning_systems"&gt;第1章&lt;/a&gt;介绍了 MLOps，在&lt;a href="ch06.html#model_independent_transformations"&gt;第6章&lt;/a&gt;介绍了数据验证测试（data validation test），在&lt;a href="ch07.html#model_dependent_and_on_demand_transform"&gt;第7章&lt;/a&gt;介绍了转换函数的单元测试（unit test）。但还有更多内容需要覆盖。如果你要构建一个可靠、可控、可维护的 AI 系统，你需要为每条 ML 管道编写集成测试（integration test），并在开发期间和部署之前运行它们。我们将研究如何编写特征管道测试（feature pipeline test）和模型验证测试（model validation test），以及如何测试模型部署。我们将研究如何在开发、预发布（staging）和生产环境中通过自动容器化（automatic containerization）可靠地打包我们的 ML 管道。我们还将介绍如何使用 &lt;em&gt;evals&lt;/em&gt;（评估）对智能体和 LLM 工作流进行离线测试。&lt;/p&gt;
&lt;p&gt;测试是构建高质量 AI 系统的关键。你的测试应该达到这样的水平：你对测试如此有信心，以至于你会在周五部署到生产环境。即使一次升级失败，你也能轻松回滚你的更改。在下一章中，我们将关注 MLOps 的运维问题，但在本章中，我们将研究开发期间运行的测试以及如何自动化 AI 系统的离线测试。&lt;/p&gt;
&lt;h1 id="离线测试"&gt;离线测试&lt;/h1&gt;
&lt;p&gt;构建可靠 AI 系统的起点是测试。AI 系统比传统软件系统需要更多的测试层级。数据或代码中的小 bug 很容易让 ML 模型默默做出错误的预测。AI 系统需要大量的工程工作来测试和验证，以确保它们产生无偏差的高质量预测。当 AI 系统部署后，它们还需要被监控，以发现坏数据、漂移（drift）、违反服务级别目标（SLO）或关键绩效指标（KPI）下降。图 &lt;a href="#the_offline_and_online_testing_pyramids"&gt;13-1&lt;/a&gt; 中的测试金字塔（testing pyramid）表明，在整个 AI 系统生命周期中，既需要离线测试，也需要在线（运行）检查。&lt;/p&gt;
&lt;p&gt;两个测试金字塔展示了 AI 系统所需的离线测试和在线测试层级，强调同时测试数据和代码以确保可靠性和性能的重要性。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/building-ml-systems/80_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;它们之所以被称为测试金字塔，是因为大多数测试位于底层（特征函数的单元测试和特征组的数据验证测试），而顶层（模型部署的蓝绿测试以及模型部署的 SLO 和 KPI）的测试较少。我们已经在&lt;a href="ch06.html#model_independent_transformations"&gt;第6章&lt;/a&gt;和&lt;a href="ch07.html#model_dependent_and_on_demand_transform"&gt;第7章&lt;/a&gt;中覆盖了两个金字塔的底层，在本章中，我们将覆盖离线测试金字塔的其余部分。&lt;/p&gt;
&lt;p&gt;这些测试金字塔可能令人望而生畏，尤其是如果你没有软件工程背景。重要的一点是，对 CI/CD 自动化测试的支持并不是开始构建 AI 系统的先决条件。自动化测试的支持可以在你构建了第一个 MVP（minimum viable product，最小可行产品）之后再加进来，以验证你所构建的东西值得维护。逐步为你构建的 AI 系统添加测试是可以的。你可以从特征函数和转换的单元测试开始，然后为特征管道和训练管道（包括模型性能和模型偏差测试）添加集成测试。然后，你可以通过添加 CI 支持来实现测试自动化，每当你向源代码仓库推送代码时就运行你的测试。&lt;/p&gt;</description></item><item><title>AI 系统的可观测性与监控</title><link>https://blog.430812.xyz/mlops/building-ml-systems/14/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/14/</guid><description>&lt;h1 id="ai-系统的可观测性与监控"&gt;AI 系统的可观测性与监控&lt;/h1&gt;
&lt;p&gt;如果你的 AI 系统足够小、活动部件少，那么一个人或许就能充分理解它，从而快速检测、诊断并修复任何问题。然而，所有成功的软件系统都会变得越来越复杂（功能蔓延！），因此需要系统支持来检测和诊断运维问题。简而言之，你的 AI 系统需要可观测性（observability）和监控（monitoring）。&lt;/p&gt;
&lt;p&gt;可观测性有两大支柱，一切皆建于其上：指标（metrics）和日志（logging）。&lt;em&gt;指标&lt;em&gt;是对基础设施服务和 ML 管道性能的数值度量。常见指标的示例包括模型性能、数据质量、延迟（latency）、吞吐量（throughput）、KPI 和成本。&lt;/em&gt;日志&lt;/em&gt;是来自基础设施服务和 ML 管道的结构化与非结构化文本输出及追踪（trace），它们提供了对内部状态、错误追踪和细粒度性能的洞察。指标是服务等级目标（service-level objective，SLO）和弹性 AI 系统的构建块，这类系统会自动扩缩容（scale up/down）其所用的资源。日志则是从错误检测、调试，到 LLM 的错误分析，再到特征和模型监控等一切工作的基础。&lt;/p&gt;
&lt;p&gt;本章涵盖本书中三类 AI 系统的可观测性与监控。我们首先研究批处理 ML 系统和实时 ML 系统的日志记录与指标。我们会看到，需要分别记录变换后（transformed）和未变换（untransformed）的特征值，分别用于特征监控和模型监控。然后，我们研究智能体（agentic）AI 系统中的可观测性，其中日志记录是错误分析（error analysis）和评估（eval）的构建块，这两者都是构建可靠智能体的关键技术。我们还将看到护栏（guardrail）如何帮助监控 LLM，防止其产生攻击性响应、泄露个人身份信息（PII）数据以及被越狱（jailbreak）。&lt;/p&gt;
&lt;h1 id="ml-模型的日志记录与指标"&gt;ML 模型的日志记录与指标&lt;/h1&gt;
&lt;p&gt;&lt;em&gt;可观测性&lt;/em&gt;是微服务社区中一个成熟术语，在那里它指的是指标、日志和追踪（tracing）（一次调用可能触及数十或数百个微服务，因此需要分布式追踪）。在 MLOps 中，可观测性主要关注指标和日志。追踪对智能体很重要，我们会记录对 LLM 和工具的调用，但它还不是分布式追踪，因此我们将 AI 系统的可观测性定义为指标和日志。&lt;/p&gt;
&lt;p&gt;&lt;a href="#batchcomma_onlinecomma_and_llms_output"&gt;图 14-1&lt;/a&gt; 展示了推理管道中的模型（批处理、在线或 LLM/智能体）如何导出指标和日志。&lt;/p&gt;
&lt;p&gt;图示说明模型如何处理预测请求，指标用于自动扩缩容和瓶颈识别，而日志用于监控、调试和解释模型决策。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/building-ml-systems/89_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;指标用于对在线模型进行自动扩缩容（扩容模型数量以满足 SLO，缩容模型数量以降低成本）。日志支撑特征/模型监控，支持调试和追踪，并支持模型决策的可解释性。我们现在依次研究批处理和实时 ML 模型的日志记录与指标，智能体/LLM 将在本章后面介绍。&lt;/p&gt;
&lt;h2 id="批处理与在线模型的日志记录"&gt;批处理与在线模型的日志记录&lt;/h2&gt;
&lt;p&gt;推理管道既产生指标也产生日志，如&lt;a href="#key_metrics_and_logs_exported_from_onli"&gt;图 14-2&lt;/a&gt;所示。指标通常存储在指标存储（metrics store）中（如 Prometheus），而推理管道的日志通常存储在表中，供下游分析和监控使用。与某次预测相关的日志在存储前应该被统一。也就是说，你应该将预测请求连同所有输入、有用的中间状态和输出存储到单一表中。统一日志将使调试模型的预测以及添加特征和模型监控支持变得更容易、更高效。&lt;/p&gt;
&lt;p&gt;图示说明在线和批处理模型推理中关键指标和日志的流向，突出它们在调试、监控特征和模型以及用于系统性能评估的追踪中的作用。&lt;/p&gt;
&lt;p&gt;&lt;img src="https://blog.430812.xyz/images/building-ml-systems/90_001.png" alt="原书插图"&gt;&lt;/p&gt;
&lt;p&gt;没有适当的日志记录和监控，调试 AI 系统是不可能的。仅仅记录模型输入和输出是不够的。你还应该记录未变换的特征数据（因为特征监控在未变换特征上效果最佳）以及调试所需的预测请求。&lt;/p&gt;
&lt;p&gt;日志数据可以存储在多种不同的数据存储中，包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;湖仓（lakehouse）表，受益于低成本存储，并且可以使用 SQL、PySpark 或 Polars/Pandas 轻松分析。这是批处理 ML 系统的好方案。&lt;/li&gt;
&lt;li&gt;带 TTL 的在线特征组（feature group），它同时包含离线湖仓表。这是实时 ML 系统的好方案。&lt;/li&gt;
&lt;li&gt;文档存储（如 OpenSearch 或 Datadog），对非结构化文本、JSON 和自由文本搜索有良好支持。&lt;/li&gt;
&lt;li&gt;关系数据库，如 Postgres，运维开销低，但在可扩展性和成本方面有挑战。&lt;/li&gt;
&lt;li&gt;SaaS 日志/监控服务，后端使用前面提到的某种数据存储。这是入门的好选择，但在成本和数据访问方面有挑战。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;对于在线日志记录，&lt;a href="#architecture_diagram_comparing_blocking"&gt;图 14-3&lt;/a&gt;展示了日志记录既可以是向 SaaS 平台的网络写入，也可以与模型部署集成以记录到特征存储，异步地同时写入实时表和湖仓表。Hopsworks 提供特征存储日志服务。&lt;/p&gt;</description></item><item><title>TikTok 的个性化推荐系统：世界上最有价值的 AI 系统</title><link>https://blog.430812.xyz/mlops/building-ml-systems/15/</link><pubDate>Sun, 09 Aug 2026 00:00:00 +0800</pubDate><guid>https://blog.430812.xyz/mlops/building-ml-systems/15/</guid><description>&lt;h1 id="tiktok-的个性化推荐系统世界上最有价值的-ai-系统"&gt;TikTok 的个性化推荐系统：世界上最有价值的 AI 系统&lt;/h1&gt;
&lt;p&gt;本章以案例研究的形式把我们迄今学到的知识汇集在一起。你将设计、构建并部署一个大规模运行、实时的个性化视频推荐系统。它的灵感来自 TikTok 的推荐系统——正是这个 AI 系统让 TikTok 通过实时 AI 创新把 YouTube 拉下了王座。我们将使用面向实时个性化 AI 系统的&lt;em&gt;检索-排序架构&lt;/em&gt;（retrieval-and-ranking architecture）来构建我们的推荐系统。我们还将扩展视频推荐系统，加入用自然语言进行视频智能体搜索（agentic search）的功能。最后，我们将以 MLOps 的&amp;quot;十二大谬误&amp;quot;（dirty dozen of fallacies）来结束本书——希望你在读完本书后不再犯这些错误——并附上一些关于你作为 AI 系统构建者所肩负的伦理责任的建议。感谢你坚持读到这里，让我们开始动手做与 AI 打交道最有成就感的部分——构建能够让世界变得更好的真实 AI 系统。&lt;/p&gt;
&lt;h1 id="推荐系统简介"&gt;推荐系统简介&lt;/h1&gt;
&lt;p&gt;&lt;em&gt;推荐系统&lt;em&gt;（recommender system）帮助用户在面向用户的系统中发现相关内容。内容可以是任何东西，从视频到音乐，从电子商务到社交媒体帖子。最早的推荐系统方法并不是个性化的。&lt;/em&gt;基于内容的推荐系统&lt;/em&gt;（content-based recommendation system）可以利用类型、导演、演员或情节关键词来推荐与用户此前观看并喜欢的视频相似的视频。训练基于内容的推荐模型只需要内容使用特征（content usage feature），因此它们很容易扩展。Netflix 和 YouTube 至今仍把基于内容的推荐作为它们提供的多种推荐类型之一。&lt;/p&gt;
&lt;p&gt;下一类推荐系统建立在&lt;em&gt;交互数据集&lt;em&gt;（interaction dataset）之上，其中包含用户对内容的操作事件，例如观看、点赞和分享。&lt;/em&gt;物品到物品（item-to-item，i2i）推荐&lt;/em&gt;关注物品本身之间的关系，从而支持&amp;quot;购买了该商品的顾客还购买了……&amp;ldquo;或&amp;quot;如果你喜欢这个视频，你可能会喜欢……&amp;ldquo;之类的功能。交互数据集提供了共同消费的模式或相似性，i2i 方法让用户可以轻松探索相关选项。&lt;/p&gt;
&lt;p&gt;&lt;em&gt;用户到物品（user-to-item，u2i）推荐&lt;/em&gt;则采取不同的方法，把推荐的中心放在单个用户身上。这里的目标是根据用户的历史偏好和行为，或借鉴相似用户的经验，向用户推荐物品。i2i 和 u2i 推荐系统第一种被广泛使用的方法是&lt;em&gt;协同过滤&lt;em&gt;（collaborative filtering），但它在处理海量数据和稀疏数据（即大多数用户只与极小一部分物品互动）时面临挑战。&lt;/em&gt;因子分解机&lt;/em&gt;（factorization machine）被引入以更好地处理数据稀疏性，但在大数据量和实时更新方面也存在可扩展性问题。&lt;/p&gt;
&lt;p&gt;在下一节中，我们将研究解决这些挑战的最先进的检索-排序架构，但我们将从查看构建推荐系统所需收集的数据开始。&lt;a href="#classes_of_features_used_in_video_recom"&gt;表 15-1&lt;/a&gt; 展示了用于训练视频推荐模型的常用特征。&lt;/p&gt;
&lt;table&gt;
	&lt;thead&gt;
			&lt;tr&gt;
					&lt;th&gt;分组&lt;/th&gt;
					&lt;th&gt;特征&lt;/th&gt;
					&lt;th&gt;变换&lt;/th&gt;
					&lt;th&gt;数据量/更新速度&lt;/th&gt;
			&lt;/tr&gt;
	&lt;/thead&gt;
	&lt;tbody&gt;
			&lt;tr&gt;
					&lt;td&gt;用户画像&lt;/td&gt;
					&lt;td&gt;性别、年龄、语言、设备、兴趣、位置、最近观看&lt;/td&gt;
					&lt;td&gt;与模型无关、与模型相关&lt;/td&gt;
					&lt;td&gt;GB/TB 级，批处理和流式&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;视频&lt;/td&gt;
					&lt;td&gt;标题、类型、时长、年龄、点击量、CTR、点赞、描述、内容&lt;/td&gt;
					&lt;td&gt;与模型无关、与模型相关&lt;/td&gt;
					&lt;td&gt;GB/TB 级，批处理和流式&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;交互&lt;/td&gt;
					&lt;td&gt;观看、跳过、点赞、分享、观看时长&lt;/td&gt;
					&lt;td&gt;与模型无关、与模型相关&lt;/td&gt;
					&lt;td&gt;TB/PB 级，批处理和流式&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;实时上下文&lt;/td&gt;
					&lt;td&gt;热门（附近的人、你的同类型人群、好友）&lt;/td&gt;
					&lt;td&gt;与模型无关、按需&lt;/td&gt;
					&lt;td&gt;GB/TB 级，流式&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;会话内浏览&lt;/td&gt;
					&lt;td&gt;设备、使用模式（连续刷视频等）、最后一次点击、会话时长&lt;/td&gt;
					&lt;td&gt;按需&lt;/td&gt;
					&lt;td&gt;GB/TB 级，实时处理&lt;/td&gt;
			&lt;/tr&gt;
			&lt;tr&gt;
					&lt;td&gt;图/社交&lt;/td&gt;
					&lt;td&gt;社交行为（如好友点赞）、社交亲密度&lt;/td&gt;
					&lt;td&gt;与模型相关、按需&lt;/td&gt;
					&lt;td&gt;GB/TB 级，批处理和流式&lt;/td&gt;
			&lt;/tr&gt;
	&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;总体而言，对构建推荐模型有用的特征围绕用户、物品（在我们的案例中是视频）以及用户与物品之间的交互展开。其中一些特征包含变化缓慢的数据，存储在数据仓库中，由批处理特征管道更新；例如，关于用户观看行为的信息，如视频的平均观看百分比，以及按视频类型压缩的观看统计。&lt;/p&gt;</description></item></channel></rss>